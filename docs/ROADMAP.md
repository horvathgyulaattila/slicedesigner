# Roadmap

Státusz: Aktív
Tulajdonos: Horváth Gyula Attila
Létrehozva: 2026-07-31
Utolsó módosítás: 2026-08-23
Kapcsolódó dokumentumok: [PROJECT_CONSTITUTION.md](PROJECT_CONSTITUTION.md), [AI_WORKFLOW.md](AI_WORKFLOW.md), [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md), [DOMAIN_MODEL.md](DOMAIN_MODEL.md)

## Cél

Ez a dokumentum meghatározza a Slice Designer fejlesztési fázisainak sorrendjét, célját és jelenlegi állapotát.

## Leírás

A ROADMAP nem backlog, nem feladatlista és nem specifikáció. A célja, hogy bármelyik AI vagy új fejlesztő azonnal lássa, hol tart a projekt, mely fázisok készültek el, mi a következő lépés, és mely dokumentumok tekinthetők lezártnak. A ROADMAP kizárólag a fejlesztési folyamatot írja le, technikai megoldásokat nem.

> **Megjegyzés (2026-08-01):** A Phase 0 korábbi ✅ Approved jelölése tévesen került rögzítésre — a kilépési feltétel ("A projekt dokumentációja önmagában értelmezhető") ténylegesen nem teljesült, mivel több Phase 0-hoz tartozó dokumentum vázlat állapotban maradt. A Phase 0 ezért 🟡 In Progress-re, a rá épülő Phase 1 pedig ⬜ Not Started-ra lett visszaminősítve.
>
> **Megjegyzés (2026-08-01, folytatás):** A Phase 0-hoz tartozó mind a hét dokumentum (PROJECT_VISION, ENGINEERING_PRINCIPLES, ARCHITECTURE, PROJECT_STRUCTURE, CODING_STANDARDS, AI_WORKFLOW, PROMPT_STANDARD) érdemi tartalommal elkészült és a projektgazda jóváhagyta — a kilépési feltétel teljesült. A Phase 0 ezért ✅ Approved-ra, a Phase 1 pedig 🟡 In Progress-re került, mivel a DOMAIN_MODEL.md már tartalmaz érdemi munkát (a Numbering-kivétel révén).
>
> **Megjegyzés (2026-08-01, folytatás 2):** A Phase 1 (Domain Design) kilépési feltétele teljesült — a DOMAIN_MODEL.md tartalmazza a koordinátarendszert, a mértékegységeket, valamint mind a 14 fogalom attribútum-szintű kiegészítését; a projektgazda a Claude Code általi végrehajtás után review-olta és jóváhagyta. A Phase 1 ezért ✅ Approved-ra, a Phase 2 (Functional Specifications) pedig 🟡 In Progress-re került.
>
> **Megjegyzés (2026-08-01, folytatás 3):** A Phase 2 (Functional Specifications) mind a nyolc tervezett specifikációja (Mesh Import, Slice Engine, Gap System, Dowel System, Backplate, Numbering, Nesting, DXF Export) elkészült és a projektgazda jóváhagyta — a kilépési feltétel teljesült. A munka során több, korábban elfogadott specifikáció és architekturális döntés is felülvizsgálatra és pontosításra került (ADR-0003–0005), amikor a részletes kidolgozás új, korábban fel nem ismert függőségeket vagy ellentmondásokat tárt fel. A Phase 2 ezért ✅ Approved-ra, a Phase 3 (Architecture Freeze) pedig 🟡 In Progress-re került.
>
> **Megjegyzés (2026-08-01, folytatás 4):** A Phase 3 (Architecture Freeze) kilépési feltétele teljesült — az architektúra és a nyolc specifikáció rendszerezett áttekintése megtörtént (ARCHITECTURE.md belső konzisztencia, DOMAIN_MODEL.md kereszthivatkozások, szétszórt backlog-bejegyzések rendezése, a teljes pipeline adatszerződéseinek végigfuttatása), a feltárt hiányosságok javításra kerültek, és a projektgazda jóváhagyta. A Phase 3 ezért ✅ Approved-ra, a Phase 4 (Implementation) pedig 🟡 In Progress-re került.
>
> **Megjegyzés (2026-08-03, folytatás 5):** A Phase 4 (Implementation) mind a nyolc, ROADMAP Phase 2-ben rögzített engine-je (Mesh Import, Slice Engine, Dowel System, Gap System, Backplate, Numbering, Nesting, DXF Export) elkészült, review-n átment, és a projektgazda jóváhagyta (89/89 automatizált teszt, `PYTHONHASHSEED=0 uv run pytest`). A munka során két, a specifikációk által Phase 4-re hagyott, keresztmetsző implementációs döntés (kontúr körüljárási irány, ADR-0007; Nesting Engine csomagolási algoritmus, ADR-0008) ADR-ben rögzítésre került. A Phase 4 ezért ✅ Approved-ra, a Phase 5 (Integration) pedig 🟡 In Progress-re került.
>
> **Megjegyzés (2026-08-04, folytatás 6):** A Phase 5 (Integration) eredeti öt tétele (modulok összekapcsolása, GUI, teljes workflow, projektmentés, beállítások) elkészült. Ezeken felül a GUI-t lezáró négy kiegészítő tétel (automatikus Mesh-előnézet, Dowel/Spacer/Backplate 3D-megjelenítés, szeletenkénti kiemelés) és két, élő tesztelés során feltárt és megvalósított javítás (Dowel automatikus pozíciókeresés térbeli szórása, Numbering Slice-oldali szigorúságának figyelmeztetés-alapúra enyhítése) is a fázis részeként valósult meg. A projektgazda jóváhagyta. A Phase 5 ezért ✅ Approved-ra, a Phase 6 (Release Candidate) pedig 🟡 In Progress-re került, első tételeként a DXF export Futtatásról való leválasztásával (önálló interakció, Döntési javaslat és Impact Analysis alatt).
>
> **Megjegyzés (2026-08-04, folytatás 7):** A Phase 6 első tétele (DXF export leválasztása a Futtatásról) a teljes munkafolyamaton átment — Döntési javaslat, Hatásvizsgálat, projektgazdai jóváhagyás, majd ADR-0009 és az ARCHITECTURE.md módosítása, ezt követően a Claude Code általi implementáció (200/200 automatizált teszt) —, és a projektgazda élő teszteléssel is megerősítette. A Phase 6 többi tétele (végső tesztelés, optimalizálás, dokumentáció, példaprojektek) még nyitott, a fázis ezért továbbra is 🟡 In Progress.
>
> **Megjegyzés (2026-08-04, folytatás 8):** A Phase 6 "optimalizálás" tétele eredetileg általános teljesítmény-átvizsgálásként szerepelt; a projektgazda pontosítása alapján a tétel elsődleges és jelenleg egyetlen tartalma a Dowel automatikus pozíciókeresés teljesítményproblémájának javítása (korábban a BACKLOG.md 2. tételeként nyilvántartva, élő tesztelés során feltárt lassúság miatt). Mivel a javítás érdemben befolyásolhatja a rendszer futásidejét és viselkedését, a projektgazda döntése alapján a "végső tesztelés" tétel ez után következik a Feladata-listán, nem előtte. A BACKLOG.md 2. tétele ezzel a ROADMAP Phase 6 hatókörébe kerül és törlésre kerül a Backlogból.
>
> **Megjegyzés (2026-08-04, folytatás 9):** A Phase 6 "optimalizálás" tétele (Dowel automatikus pozíciókeresés teljesítmény-javítása) lezárult. A megoldás: (1) a rácspontonkénti kör-poligon építés (`buffer().within()`) cseréje régiónkénti, egyszeri Minkowski-erózió-előszámításra és olcsó pont-tartalmazás tesztre, egy explicit, dokumentált tangencia-tolerancia (`_EROSION_TANGENCY_EPSILON_MM`) bevezetésével a karakterizációs teszt bit-pontos megőrzéséhez; (2) a `DOWEL_SYSTEM_SPEC.md` 6. szakaszának kiegészítése egy teljes sziget-lefedettségi szabállyal — a legszűkösebb (legkevesebb érvényes jelölttel rendelkező) szigetek elsőbbséget élveznek a lefedésben, felső korlát nélkül, majd a fennmaradó helyek a célszámig (`dowel_count_per_region`) sűrűsödnek; a soha le nem fedhető, illetve a szűkösségi verseny miatt kiszorult szigetek külön, megkülönböztetett figyelmeztetést kapnak (nem hibát); (3) elágazás-detektálással a drágább, szigetenkénti számítás csak ott fut, ahol ténylegesen szükséges (elágazás-mentes régióban olcsó, poligon-teszt nélküli szűréssel helyettesítve). A Claude Code általi implementáció (203/203 automatizált teszt, ebből 3 új a lefedettségi logikára) és a projektgazda élő tesztelése egyaránt megerősítette a működést.
>
> **Megjegyzés (2026-08-08, folytatás 10):** Egy rendkívül hosszú, egyetlen munkamenetben lezajlott "végső tesztelés" valódi felhasználói modelleken (elsősorban "Wobbly Toad" és "face-in-the-brick-wall" STL) végzett élő tesztelése számos valódi hibát tárt fel és javított ki a Slice/Numbering/Gap/Backplate Engine-ekben — a részletes felsorolás a Phase 6 szakaszban található. Egy tétel nyitva maradt: a Backplate-feliratok karakter-szintű tükröződése (`numbering_engine.py::_glyph_point_rect()`), amely ugyanabba a hibaosztályba tartozik, mint a már javított szelet-oldali `_glyph_to_local()`, de önálló, korábban sosem javított duplikátum. A gyökérok azonosítva, a javítás megtervezve és sandbox-környezetben vizuálisan ellenőrizve, de Claude Code-nak még nem lett átadva végrehajtásra — ez az azonnali következő lépés egy új beszélgetésben. Emellett a szelet-kontúr korábbi, a projektgazda által soha nem megerősített "nézőpont" magyarázata (miszerint a DXF-en látott tükröződés csak a lap másik oldaláról nézés természetes következménye) validálatlan marad, és a Backplate-felirat hibájának fényében újra, bizonyítékkal (nem érveléssel) megvizsgálandó. A Phase 6 állapota emiatt továbbra is 🟡 In Progress.
>
> **Megjegyzés (2026-08-08, folytatás 11 — a "NYITOTT" tételek gyökérok-javítása):** Egy célzott, kizárólag erre a témára fókuszáló audit (nem egy általános "rendszerszintű" átvizsgálás) mérve és levezetve (nem feltételezve) azonosította a szelet-kontúr és a Backplate (kontúr ÉS felirat) tükröződésének közös gyökérokát: a `slice_engine.py::_TO_2D_ROTATION` a "folytatás 7" bejegyzésben leírt korábbi javítás során SZÁNDÉKOSAN, dokumentáltan csak forgatást (determináns +1) tartalmazott, tükrözés nélkül — ez maga volt a hiba, nem a "nézőpont" (lap másik oldaláról nézés). Egy diagnosztikai script és a projektgazda saját, korábbi (javítás előtti) kódverziója egyaránt igazolta, hogy a helyes vetítéshez pontosan egy tükrözés szükséges. Emellett az audit egy **korrekciót** is rögzít a "folytatás 10" bejegyzés (illetve az azt megelőző "rendszerszintű audit") állításával szemben: az akkori audit azt állította, hogy a glyph-tükrözési hibaosztálynak **kizárólag két** előfordulása van (`numbering_engine.py::_glyph_to_local`, `numbering_engine.py::_glyph_point_rect`) — ez tévedés volt. A jelen audit egy **harmadik, korábban fel nem ismert előfordulást** is talált: `nesting_engine.py::_glyph_point_rect()` (a toldási/seam al-azonosítók rajzolásánál), amelynek `non-upright` ága szintén determináns -1 (tükrözött) volt — ez azért maradhatott korábban észrevétlen, mert a meglévő tesztek csak a szöveg illeszkedését ellenőrizték, nem a tükrözöttségét. A gyökérok-javítás (ADR-0010) részeként: (1) a `_TO_2D_ROTATION` mindhárom mátrixa szándékos, egységes tükrözést kapott; (2) a korábban egymástól függetlenül duplikált `_SLICE_AXIS_CONTOUR_ORDER`/`_NORMAL_AXIS_WORLD` táblák egyetlen, megosztott forrásra kerültek (`slice_engine.py`, illetve `backplate_engine.py`); (3) a `backplate_engine.py::_build_backplate_shape_from_mesh()` mostantól ténylegesen felhasználja a `backplate_normal_axis` előjelét (korábban explicit módon eldobta — `_sign`, nem használt változó), ugyanezt az előjelet alkalmazva a fészek-kivágásnál és a Backplate-oldali azonosító horgonypont-számításánál is (`apply_numbering_to_backplate()` emiatt új, kötelező `backplate_normal_axis` paramétert kapott); (4) a Backplate-oldali és a most felismert seam-oldali `_glyph_point_rect()` mindkettő javítva, determináns +1-re. A Claude Code általi implementáció (229/229 automatizált teszt, ebből 5 új a determináns-/tükrözés-regresszióra) megtörtént. Ezzel a Phase 6 "NYITOTT" szakaszának mindkét tétele (1. a szelet-kontúr "nézőpont" kérdése — cáfolva, a valódi gyökérok azonosítva és javítva; 2. a Backplate-felirat tükröződése — javítva, plusz egy harmadik, korábban fel nem ismert előfordulás is javítva) implementációs szempontból lezártnak tekinthető, a projektgazdai élő tesztelés (a DXF export puszta vágási vonalán és feliratain, a fizikai modellel összevetve) megerősítésére várva — ezt a projektgazda a review lépésben erősíti meg. A Phase 6 állapota emiatt továbbra is 🟡 In Progress (a "dokumentáció" és "példaprojektek" tételek is nyitottak maradnak).
>
> **Megjegyzés (2026-08-09, folytatás 12):** A Phase 6 hátralévő "dokumentáció" és "példaprojektek" tétele konkrét, 9 alpontos struktúrát kapott (6.1–6.9), a projektgazda jóváhagyásával. A dokumentáció felhasználói kézikönyvre (`USER_GUIDE.md`), workflow-leírásra (`WORKFLOW.md`), a meglévő technikai dokumentáció véglegesítésére és release dokumentációra (`RELEASE_NOTES.md`) tagolódik; a példaprojektek négy, leíró nevű mappára (`basic_example/`, `complex_example/`, `nesting_example/`, `reference_project/`) és egy validálási tételre. A Feladata-lista és a Phase 6 Kilépési feltétele ennek megfelelően frissült.
>
> **Megjegyzés (2026-08-09, folytatás 13):** A Phase 6 (Release Candidate) mind a 9 alpontja (6.1–6.9) elkészült, review-n átment és a projektgazda jóváhagyta: felhasználói kézikönyv (`USER_GUIDE.md`), workflow-leírás (`WORKFLOW.md`), technikai dokumentáció audit és javítás (README/ARCHITECTURE/PROJECT_STRUCTURE + 3 specifikáció, 6 elavult hivatkozás/dátum javítva), release dokumentáció (`RELEASE_NOTES.md`), négy ténylegesen generált és futtatott példaprojekt (`examples/basic_example/`, `complex_example/`, `nesting_example/`, `reference_project/` — mindegyik saját `generate_example.py`-jal reprodukálható), és ezek összesített validálása (törlés utáni friss újragenerálás, mind a négynél hibátlan, dokumentumhű eredménnyel). A Kilépési feltétel teljesült. A Phase 6 ezért ✅ Approved-ra került — ezzel a ROADMAP mind a hét fázisa (Phase 0–6) lezárult.
>
> **Megjegyzés (2026-08-13, folytatás 14):** A projektgazdával lezajlott backlog-áttekintés nyomán a `BACKLOG.md` mindhárom tétele lezárásra került: a `[project.scripts]` bejegyzés (2. tétel) törölve, nem valósul meg; a 2D export-előnézet (1. tétel) és a Dedikált "Példák megnyitása" GUI-funkció (3. tétel) átkerült az újonnan megnyitott Phase 7-be. A projektgazda emellett két új tételt vett fel Phase 7-be: a Nesting true-shape elrendezését (a korábban elfogadott ADR-0008 felülvizsgálatával) és egy GUI-átalakítást (lapozható, fülsávos paraméter-kártyák; önálló, állítható magasságú log-panel; Futtatás/Export gombok áthelyezése). A Phase 7 ezért 🟡 In Progress állapotban megnyílt.
>
> **Megjegyzés (2026-08-13, folytatás 15):** A Phase 7 7.1 tételéhez (2D export-előnézet) elkészült és a projektgazda jóváhagyta a specifikációt (`docs/specifications/EXPORT_PREVIEW_SPEC.md`) — a `PreviewPanel` új, nagyítható/mozgatható 3D/2D nézet-váltása, a legutóbbi sikeres Futtatás `Nest`-jeinek laponkénti megjelenítésével, a lap fizikai méretét háttérként megjelenítve. A tétel implementációja még hátravan.
>
> **Megjegyzés (2026-08-13, folytatás 16):** A Phase 7 7.1 tétele (2D export-előnézet) elkészült, review-n átment és élő teszttel megerősítve a projektgazda jóváhagyta: a `PreviewPanel` új 3D/2D nézet-váltóval, laponkénti navigációval, nagyítható/mozgatható `QGraphicsView`-alapú rajzolással bővült (`preview_panel.py`), a `MainWindow._on_pipeline_succeeded()` a `Nest`-eket és az anyag-lapméreteket adja át neki (`main_window.py`). 10 új teszt a `test_preview_panel.py`-ban, bővült esetek a `test_main_window.py`-ban; `ruff format`/`ruff check`/`mypy` hibamentes; a teljes csomag (256 teszt) regresszió nélkül zöld.
>
> **Megjegyzés (2026-08-13, folytatás 17):** A Phase 7 7.2 tételéhez (Példák megnyitása) elkészült és a projektgazda jóváhagyta a specifikációt (`docs/specifications/EXAMPLES_LAUNCHER_SPEC.md`) — a "Fájl" menü új "Példák megnyitása..." akciója egy listázó dialógust nyit, majd a kiválasztott példa `generate_example.py`-ját háttérben újrafuttatva ("regenerálás + megnyitás", a példaprojektek dokumentáltan gépfüggő útvonalai miatt) tölti be a friss projektfájlt, a meglévő "Projekt megnyitása" logikájával. A tétel implementációja még hátravan.
>
> **Megjegyzés (2026-08-13, folytatás 18):** A Phase 7 7.2 tétele (Példák megnyitása) elkészült, review-n átment és élő teszttel megerősítve a projektgazda jóváhagyta: új `examples_dialog.py` modul (`discover_examples()` README-parszolással, `ExamplesDialog` listázó felület, `ExampleGenerationWorker` háttérszálas `generate_example.py`-futtatással), a `main_window.py` "Fájl" menüje "Példák megnyitása..." akcióval bővült, a betöltés a meglévő "Projekt megnyitása" logikáját használja újra, konkurencia- és ablakbezárás-védelemmel. 22 új teszt; `ruff format`/`ruff check`/`mypy` hibamentes; a teljes csomag (278 teszt) regresszió nélkül zöld.
>
> **Megjegyzés (2026-08-13, folytatás 19):** A Phase 7 7.3 tételéhez (Nesting true-shape elrendezés) elkészült és a projektgazda jóváhagyta az ADR-0013-at, ami felváltja az ADR-0008-at — a csomagolási algoritmus Bottom-Left Fill heurisztikára vált, valódi Shapely-kontúr-ütközéssel (nem befoglaló téglalappal), 45°-os szögkészlettel és első-illeszkedő elfogadással. A vizsgálat során kiderült, hogy a `NESTING_SPEC.md` nem igényel módosítást (a specifikáció szövege mindvégig ezt az állapotot írta elő) — a ROADMAP 7.3 sora és Kilépési feltétele ennek megfelelően pontosításra került. A tétel implementációja még hátravan.
>
> **Megjegyzés (2026-08-13, folytatás 20):** A Phase 7 7.3 tétele (Nesting true-shape elrendezés) elkészült, review-n átment és élő teszttel megerősítve a projektgazda jóváhagyta: a `nesting_engine.py` csomagolási algoritmusa Bottom-Left Fill (BLF) heurisztikára cserélve, valódi Shapely-kontúr-ütközéssel, az ADR-0013 szerint. Az implementáció közben egy szükséges korrekció történt a jelölt-pozíciók generálásában (a "kifelé néző" befoglaló téglalap-sarkak `kerf_mm`-mel eltolva szerepelnek a rácsban — enélkül a szomszédos elhelyezés `kerf_mm > 0` mellett sosem teljesült volna), amit review-ra a projektgazda elfogadott. 5 új teszt, köztük egy, ami számszerűen bizonyítja a true-shape-előnyt (két egymásba kulcsolódó háromszög egy lapon fér el, ahol a régi, befoglaló-téglalap alapú algoritmus két lapot igényelt volna); `ruff format`/`ruff check`/`mypy` hibamentes; a teljes csomag (283 teszt) regresszió nélkül zöld, kétszeri `PYTHONHASHSEED=0` futtatás bitre azonos eredménnyel.
>
> **Megjegyzés (2026-08-13, folytatás 21):** A Phase 7 7.4 tételéhez (GUI-átalakítás) a projektgazda jóváhagyta a teljes tervet: a `ParameterPanel` 7 funkciócsoportja + a `RunPanel`-ból kiemelt DXF Export paraméterek együtt 8, fenti fülsávos (tab), lapozható kártyává alakulnak (a Dowel/Gap/Backplate be-/kikapcsolók a saját kártyájukon belül maradnak); a log/állapot-terület önálló, húzással állítható magasságú `QSplitter`-panellé válik; a Futtatás/DXF Export gombok a `ParameterPanel` oszlop aljára kerülnek. A vizsgálat során kiderült, hogy nincs szükség ARCHITECTURE.md-módosításra (a GUI-szakasz kizárólag felelősség-szintű, ezt egy belső elrendezés-változás nem érinti) — a Phase 7 Kilépési feltétele ennek megfelelően pontosításra került. A tétel implementációja még hátravan.
>
> **Megjegyzés (2026-08-13, folytatás 22):** A Phase 7 7.4 tétele (GUI-átalakítás) elkészült és a projektgazda élő tesztelése megerősítette: a `ParameterPanel` 8 fülre tagolva (`parameter_panel.py`), a `RunPanel` három elkülönített konténerre bontva (`run_panel.py`: export-beállítások, akció-gombok, log), a `MainWindow` új, kétszintű `QSplitter`-elrendezéssel (`main_window.py`). A `config_builder.py`/`config_loader.py` érintetlen maradt, a kritikus attribútum-invariáns megtartva. Az élő tesztelés három kiegészítő vizuális finomítást hozott, mindegyiket a projektgazda jóváhagyta és élőben megerősítette: a Futtatás gomb zöld kiemelést kapott; a "Szeletelt összeállítás" nézet alap-teste RAL 8003-ra (`#7E4B26`), élkontúrja RAL 8022-re (`#1A1719`) színeződött (a nyers Mesh és a kiemelt szelet nézet változatlan). A teljes tesztkészlet minden körben zöld maradt, `ruff`/`mypy` hibamentes. **Ezzel a Phase 7 mind a négy tétele (7.1–7.4) lezárult.**
>
> **Megjegyzés (2026-08-13, folytatás 23):** A `MESH_IMPORT_SPEC.md` `origin_alignment` paramétere egy új értékkel bővült: `min_corner` (a bounding box minimum sarkának origóba tolása). A projektgazda kifejezett kérésére ez NEM önálló ROADMAP-fázis vagy Backlog-tétel — kis, jól körülhatárolt, egy körben lezárt kiegészítés a már meglévő, dokumentáltan nyitva hagyott paraméteren. `mesh_import.py` és `parameter_panel.py` bővült, tesztekkel lefedve.
>
> **Megjegyzés (2026-08-17, folytatás 24):** A Phase 8 (Relief Generator Plugin) mind a hét tétele elkészült, review-n átment és a projektgazda élő tesztelésével megerősítve: a MeshSource architekturális döntés (ADR-0014), az opcionális plugin-architektúra (ADR-0015) és a repository-struktúra (ADR-0016) a tervezési szakaszban lezárult; a plugin domain-, geometria- és mesh-generálási modellje (`docs/plugins/relief_generator/`) mind Elfogadva státuszban véglegesítve; az első implementáció (Wave Generator → Height Field → ReliefGeometry → Mesh Generator → Mesh → MeshSource) automatizált teszttel lefedve elkészült; a plugin discovery-mechanizmusa és a hozzá tartozó generikus GUI paraméter-séma (`MeshSourceDescriptor`, ADR-0017) a projektgazda kifejezett kérésére bevezetésre került, ezzel a valódi, szabálysértés nélküli Qt GUI-integráció (Forrás-választó a "Mesh Import" fülön, generikus `ParameterSpec`-form, háttérszálas generálás) megvalósult a `ParameterPanel`/`MainWindow`-ban. A projektgazda élő tesztelése megerősítette, hogy a generált modell hibátlanul végigfut a teljes downstream pipeline-on (Slice Engine-től a Nesting Engine-ig). A Kilépési feltétel teljesült — minden korábban `docs/drafts/relief_generator_plugin/` alatti dokumentum már a tervezési szakaszban átkerült a végleges `docs/plugins/relief_generator/` alá, Elfogadva státusszal. A Phase 8 ezért ✅ Approved-ra került. A projektgazda jelezte, hogy egy jövőbeli fázis fogja tartalmazni a további generátor-típusokat (pl. Heightmap, Image, Vector Generator) — ennek tervezete még nem készült el, a ROADMAP-ban emiatt egyelőre nem nyílik meg új fázis.
>
> **Megjegyzés (2026-08-19, folytatás 25):** A projektgazdával lezajlott tervezési egyeztetés nyomán megnyílt a Phase 9 (Wave Extension) — a Phase 8-ban elfogadott Directional Wave Generator matematikai domainmodelljének bővítése. A fázis hét tételre bomlik: 9.1 Wave model extension, 9.2 Spatial amplitude envelope, 9.3 Radial wave source, 9.4 Multiple wave sources, 9.5 Wave combination / weighting, 9.6 Controlled procedural distortion, 9.7 Integration, validation, documentation. A 9.1, 9.2, 9.3 és 9.5 tételekhez tartozó tervezési dokumentum (`docs/drafts/relief_generator_wave_extension/` alatt) ezzel egy időben, Tervezet státusszal elkészült. A 9.4 (explicit, felhasználói szinten megadható hullámforrás-lista — a projektgazda kifejezett kérésére, mert a puszta domain-szintű validáció nem tenné lehetővé a radiális hullámforrások gyakorlati konfigurálását) és a 9.6 (koordináta-torzítás a PropagationModel előtt, önálló Distortion komponensként) hatóköre és iránya rögzítésre került, de a részletes séma külön, önálló tervezési körben készül el a saját alfázisuk aktívvá válásakor. A Phase 9 ezért 🟡 In Progress állapotban nyílt meg, első aktív tételként a 9.1 (Wave model extension) tervezetének review-jával.
>
> **Megjegyzés (2026-08-19, folytatás 26):** A Szoftverarchitekt elvégezte a Phase 9 megnyitásának review-ját — mind a hét érintett fájl (a `docs/ROADMAP.md` diffje és a hat `docs/drafts/relief_generator_wave_extension/` alatti dokumentum) szó szerint megegyezett a jóváhagyott prompttal. A projektgazda ezt követően — a Phase 8 mintáját követve, ahol a design-dokumentumok az implementáció előtt kerültek véglegesítésre — jóváhagyta a 9.1, 9.2, 9.3 és 9.5 tervezési dokumentumok véglegesítését: `WAVE_DOMAIN_MODEL.md`, `AMPLITUDE_ENVELOPE.md`, `RADIAL_WAVE_SOURCE.md` és `WAVE_WEIGHTING.md` `Tervezet` helyett `Elfogadva` státusszal átkerült `docs/drafts/relief_generator_wave_extension/` alól a végleges `docs/plugins/relief_generator/` alá, a kereszthivatkozások (a most már testvérfájl `WAVE_FUNCTION_MODEL.md`, illetve a továbbra is draft állapotban maradó `MULTIPLE_WAVE_SOURCES.md`/`PROCEDURAL_DISTORTION.md`) útvonalának megfelelő frissítésével. A 9.4 (Multiple wave sources) és a 9.6 (Controlled procedural distortion) továbbra is vázlat státuszban, `docs/drafts/relief_generator_wave_extension/` alatt marad — a mappa emiatt nem törlődik. A négy véglegesített dokumentum tényleges Python-implementációja a Phase 9 következő lépése.
>
> **Megjegyzés (2026-08-19, folytatás 27):** A 9.4 (Multiple wave sources) részletes tervezése lezárult — a projektgazdával folytatott egyeztetés mind a négy nyitott kérdést megválaszolta: (1) a meglévő, `WaveParameters`-alapú automatikus, jitterelt `Directional` komponens-generálás és az új, explicit forráslista NEM egymást kizáró alternatívák, hanem egymást kiegészítő mechanizmusok, amelyek ugyanabba a `WaveSet`-be táplálnak; (2) egy explicit forrás specifikációja (`WaveSourceSpec`) a `source_type` (Directional/Radial), a típus-specifikus pozíció (irány, illetve source X/Y), valamint amplitude/wavelength/phase/weight mezőkből áll, rögzített Sinusoidal WaveFunction és Uniform AmplitudeEnvelope mellett; (3) a végső `WaveSet` az automatikusan generált komponensek és az explicit forráslista determinisztikus összefűzéséből áll (automatikus előbb, explicit utána); (4) nincs önálló, több-forrásra vonatkozó validációs szabály — két azonos centerű Radial forrás érvényes konfiguráció. A `MULTIPLE_WAVE_SOURCES.md` (`docs/drafts/relief_generator_wave_extension/` alatt) ezzel a tartalommal frissült, továbbra is `Tervezet` státuszban.
>
> **Megjegyzés (2026-08-19, folytatás 28):** A 9.6 (Controlled procedural distortion) részletes tervezése lezárult. A projektgazdával folytatott egyeztetés tisztázta, hogy a felmerült torzítás-kategóriák (frequency, phase, direction, radial distortion) mind ugyanannak az egyetlen, már rögzített mechanizmusnak (koordináta-warp a PropagationModel előtt) a speciális esetei, külön tervezés nélkül; az Amplitude distortion (magasság-moduláció) ezzel szemben fogalmilag az AmplitudeEnvelope (9.2) körébe tartozik, ezért kikerült a 9.6-ból és a BACKLOG.md-be került (3. tétel). A Distortion architekturálisan per-Wave komponens marad (WAVE_DOMAIN_MODEL.md 16.4 szakasza szerint, nem globális "Distortion Layer"). A 9.6 első köre — a WaveFunction-nél (BACKLOG.md 1. tétel) alkalmazott mintát követve — egyetlen konkrét típust vezet be: a SwirlDistortion-t (középpont körüli, távolsággal csökkenő mértékű elforgatás); a további torzítás-típusok (noise-alapú koordináta-warp, multi-scale) a BACKLOG.md-be kerültek (4. tétel). A PROCEDURAL_DISTORTION.md (docs/drafts/relief_generator_wave_extension/ alatt) ezzel a tartalommal frissült, továbbra is Tervezet státuszban.
>
> **Megjegyzés (2026-08-19, folytatás 29):** A projektgazda jóváhagyta a 9.4 és a 9.6 tervezési dokumentumok véglegesítését, a 9.1/9.2/9.3/9.5 mintáját követve: `MULTIPLE_WAVE_SOURCES.md` és `PROCEDURAL_DISTORTION.md` `Tervezet` helyett `Elfogadva` státusszal átkerült `docs/drafts/relief_generator_wave_extension/` alól a végleges `docs/plugins/relief_generator/` alá. Mivel ezzel a hat Phase 9 tervezési dokumentum mindegyike véglegesítve lett, a `docs/drafts/relief_generator_wave_extension/` mappa a `PROJECT_STRUCTURE.md` kilépési szabálya szerint törlésre került. A már korábban Elfogadva `WAVE_DOMAIN_MODEL.md` hét, a draft mappára mutató hivatkozása (a most testvérfájllá váló `MULTIPLE_WAVE_SOURCES.md` és `PROCEDURAL_DISTORTION.md` felé) ennek megfelelően testvérfájl-hivatkozássá egyszerűsödött — ez kizárólag hivatkozás-útvonal javítás, tartalmi/architekturális módosítás nélkül. A Phase 9 mind a hat domain-contract dokumentuma (9.1–9.6) ezzel Elfogadva státuszban, a végleges helyén van; a hátralévő munka a Python-implementáció (9.1–9.6) és a 9.7 (Integration, validation, documentation) megtervezése.
>
> **Megjegyzés (2026-08-19, folytatás 30):** A Phase 9 dokumentációs szakasza ezzel lezárult — a 9.7 (Integration, validation, documentation) ROADMAP-tétele kibővült a konkrét hatókörrel: a 9.1–9.6 domain-contract Python-implementációja, alfázisonkénti automatizált teszt, backward compatibility teszt a Phase 8 WaveGenerator ellenében, end-to-end integrációs teszt és élő tesztelés a projektgazdával. A 9.7-nek — a többi alfázistól eltérően — nincs önálló domain-contract dokumentuma, mivel nem vezet be új domain fogalmat, tisztán implementációs/validációs feladat, a Phase 8 IMPLEMENTATION_PLAN.md-jának mintájára. Ezzel a Phase 9 mind a hét tétele dokumentáltan rögzítve van; a hátralévő munka kizárólag implementáció.
>
> **Megjegyzés (2026-08-20, folytatás 31):** A Szoftverarchitekt Döntési javaslatot terjesztett elő a 9.1–9.6 domain-contractok implementációs sorrendjére és tesztelési stratégiájára, amit a projektgazda jóváhagyott: a végrehajtás sorrendje **9.1 → 9.2 → 9.3 → 9.5 → 9.4 → 9.6** (a ROADMAP eredeti sorrendjétől egyetlen eltéréssel — a 9.5 a 9.4 elé kerül, mert a `WaveSourceSpec` (9.4) `weight` mezője a 9.5-ben rögzített szemantikára épül; a 9.4 emellett technikai okból is csak 9.3 után következhet, mivel a `WaveSourceSpec` `Radial` ága a `RadialPropagation`-re épül). A Hatásvizsgálat során kiderült, hogy a Phase 8 mintáját követő új terv-dokumentum nem kaphatja az `IMPLEMENTATION_PLAN.md` nevet, mert ez a fájlnév a `docs/plugins/relief_generator/` alatt már foglalt (a Phase 8 első implementációs terve) — a projektgazda jóváhagyásával az új dokumentum `docs/plugins/relief_generator/WAVE_EXTENSION_IMPLEMENTATION_PLAN.md` néven jött létre, `Elfogadva` státusszal, a sorrendet, az alfázisonkénti tesztelési stratégiát, a Phase 8 elleni backward compatibility tesztet és az end-to-end integrációs teszt tervét rögzítve. A tényleges Python-implementáció a Phase 9 következő lépése.
>
> **Megjegyzés (2026-08-20, folytatás 32):** A 9.1 (Wave model extension) implementációja elkészült és review-n átment. A `WAVE_DOMAIN_MODEL.md` 2. és 12. szakasza pontosításra került (a Szoftverarchitekt a tervezés során talált ellentmondást a 4.1/8.1/16.5 és a 12. szakasz között — az `AmplitudeEnvelope` Wave-szintű komponens, nem a `WaveSet`-re alkalmazott, normalizálás utáni globális lépés; a projektgazda a Per-Wave értelmezést hagyta jóvá). Az új `plugins/relief_generator/domain/wave.py` bevezette a `WaveFunction`/`Sinusoidal`, `PropagationModel`/`DirectionalPropagation`, `AmplitudeEnvelope`/`UniformEnvelope`, `Wave` és `WaveSet` osztályokat; a `WaveGenerator` a korábbi `_WaveComponent`-alapú belső reprezentáció helyett ezekre épül, a komponens-előállítási képletek változatlanok. A backward compatibility empirikus bizonyítéka: a meglévő `test_wave_generator.py` egyetlen sora sem módosult, és továbbra is hibátlanul lefut. A teljes `tests/plugins/relief_generator/` tesztkészlet 87/87 teszttel zöld (`PYTHONHASHSEED=0 uv run pytest`), `ruff format`/`ruff check`/`mypy --strict` hibamentes. A hátralévő munka a Phase 9 következő lépése: 9.2 (Spatial amplitude envelope), a `docs/plugins/relief_generator/WAVE_EXTENSION_IMPLEMENTATION_PLAN.md`-ben rögzített sorrend szerint.
>
> **Megjegyzés (2026-08-20, folytatás 33):** A 9.2 (Spatial amplitude envelope) implementációja elkészült és review-n átment. Az új `plugins/relief_generator/domain/amplitude_envelope.py` bevezette a `Falloff` Protocolt és három megvalósítását (`LinearFalloff`, `SmoothFalloff`, `GaussianFalloff`), valamint a `RadialAmplitudeEnvelope`-ot, amely a `wave.py` `AmplitudeEnvelope` Protocolját strukturális típusozással (Protocol/duck typing) elégíti ki, annak módosítása nélkül. A lépés tisztán additív volt: sem a `WaveGenerator`, sem a `wave.py` nem változott, mivel sem az automatikus generálás, sem a (később érkező) explicit forráslista nem használ egyelőre mást, mint Uniform envelope-ot (`AMPLITUDE_ENVELOPE.md`, `MULTIPLE_WAVE_SOURCES.md` 4. szakasz) — a `RadialAmplitudeEnvelope` egy önállóan tesztelt, jövőbeli konfigurációnak szánt domain-építőelem. A teljes `tests/plugins/relief_generator/` tesztkészlet 102/102 teszttel zöld, `ruff`/`mypy --strict` hibamentes. A projektgazdával egyeztetve: a jelenlegi `ParameterSpec` (ADR-0017) mechanizmus kizárólag skalár típusokat (float/int/str/enum) támogat, és nincs felkészítve a 9.4 (`WaveSourceSpec`) által igényelt, változó hosszúságú strukturált listára — ezt a kérdést a projektgazda döntése szerint a 9.7 (Integration) oldja meg, nem korábban. A hátralévő munka a Phase 9 következő lépése: 9.3 (Radial wave source), a `WAVE_EXTENSION_IMPLEMENTATION_PLAN.md`-ben rögzített sorrend szerint.
>
> **Megjegyzés (2026-08-20, folytatás 34):** A 9.3 (Radial wave source) implementációja elkészült és review-n átment. Az új `plugins/relief_generator/domain/radial_wave_source.py` bevezette a `RadialPropagation`-t, amely a `wave.py` `PropagationModel` Protocolját strukturális típusozással elégíti ki. A `RADIAL_WAVE_SOURCE.md` domain-contractja a `source_x`/`source_y` mezőkre nem ír elő invariánst, ezért ehhez a lépéshez nem tartozott új kivétel — az `exceptions.py` változatlan maradt. A lépés — a 9.2-höz hasonlóan — tisztán additív volt: sem a `WaveGenerator`, sem a `wave.py` nem változott, mivel a `RadialPropagation` egyelőre sem az automatikus generálásba, sem a (még nem implementált) explicit forráslistába nincs bekötve. A teljes `tests/plugins/relief_generator/` tesztkészlet 106/106 teszttel zöld, `ruff`/`mypy --strict --explicit-package-bases` hibamentes. A hátralévő munka a Phase 9 következő lépése: 9.5 (Wave combination / weighting), a `WAVE_EXTENSION_IMPLEMENTATION_PLAN.md`-ben rögzített sorrend szerint.
>
> **Megjegyzés (2026-08-20, folytatás 35):** A 9.5 (Wave combination / weighting) lezárult, a korábbiaktól eltérő jelleggel: a Szoftverarchitekt a Döntési javaslat előkészítésekor megállapította, hogy a `WAVE_WEIGHTING.md` domain-contractja (weight ≠ amplitude, `weight = 0` érvényessége, negatív weight előjelváltása, `WaveSet` súlyozott összegzése, weight a normalizálás előtti érvényesülése) már teljes egészében implementálva volt a 9.1 (`Wave`/`WaveSet`) részeként — ehhez a lépéshez ezért nem tartozott production kód. Az egyetlen hiányzó elem a `WAVE_EXTENSION_IMPLEMENTATION_PLAN.md`-ben vállalt, dedikált, `WaveSet`-szintű (több komponensű) tesztlefedettség volt, amit az új `tests/plugins/relief_generator/domain/test_wave_weighting.py` pótolt: nulla súlyú komponens gyűjteménybeli megmaradása és hatástalansága, ellentétes súlyú komponensek destruktív interferenciája, vegyes súlyok lineáris összegzése, és a weight nyers `F(x,y)`-ra gyakorolt (normalizálás előtti) hatása. A teljes `tests/plugins/relief_generator/` tesztkészlet 111/111 teszttel zöld, `ruff` hibamentes. A hátralévő munka a Phase 9 következő lépése: 9.4 (Multiple wave sources), a `WAVE_EXTENSION_IMPLEMENTATION_PLAN.md`-ben rögzített sorrend szerint.
>
> **Megjegyzés (2026-08-20, folytatás 36):** A 9.4 (Multiple wave sources) implementációja elkészült és review-n átment — ez az első lépés, amely ténylegesen új, konfigurálható production kódot ad a `WaveSet` felépítéséhez, nem csak önmagában álló domain-építőelemet. Az új `plugins/relief_generator/domain/multiple_wave_sources.py` bevezette a `WaveSourceSpec`-et (fail-fast validációval: `amplitude`/`wavelength` pozitivitás, valamint a típus-specifikus mezők — `direction`, illetve `source_x`/`source_y` — kölcsönös kizárása a `source_type`-nak megfelelően), a `build_wave`-et (egy specifikációból konkrét `Wave`, rögzített Sinusoidal/Uniform mellett) és a `build_combined_wave_set`-et (automatikus és explicit komponensek determinisztikus összefűzése, automatikus előbb). A `MULTIPLE_WAVE_SOURCES.md` 7. szakasza szerint, és a projektgazdával a 9.2 lezárásakor egyeztetve, a tényleges `WaveParameters`/`WaveGenerator`/GUI-bekötés a 9.7 (Integration) hatóköre marad — ez a lépés is tisztán additív volt, a `WaveGenerator`, a `wave.py`, az `amplitude_envelope.py`, a `radial_wave_source.py` és a `wave_parameters.py` egyike sem változott. Explicit teszt igazolja a Phase 8/9.1 backward compatibility-t üres explicit forráslista esetén, valamint két azonos centerű `Radial` forrás érvényességét. A teljes `tests/plugins/relief_generator/` tesztkészlet 126/126 teszttel zöld, `ruff`/`mypy --strict --explicit-package-bases` hibamentes. A hátralévő munka a Phase 9 következő lépése: 9.6 (Controlled procedural distortion), a `WAVE_EXTENSION_IMPLEMENTATION_PLAN.md`-ben rögzített sorrend szerinti utolsó domain-contract lépés, majd a 9.7 (Integration).
>
> **Megjegyzés (2026-08-20, folytatás 37):** A 9.6 (Controlled procedural distortion) implementációja elkészült és review-n átment — ez volt az első lépés 9.1 óta, amely magát a `wave.py`-t módosította: új `Distortion` Protocol és a `Wave` osztály új, opcionális `distortion: Distortion | None = None` mezője (a `weight` után, alapértékkel, ezért a meglévő, kizárólag kulcsszavas argumentumokkal hívott konstrukciók nem törtek), valamint az `evaluate()` bővítése — a `distortion` (ha meg van adva) kizárólag a `propagation` bemenetét torzítja, az `envelope`-ét nem, a `PROCEDURAL_DISTORTION.md` 2.1 szakaszának képlete szerint. Az új `plugins/relief_generator/domain/procedural_distortion.py` bevezette a `SwirlDistortion`-t. A `tests/plugins/relief_generator/domain/test_wave.py` kivételesen bővült (nem csak új fájl jött létre): a fájl végéhez két új teszt került, a meglévő tartalom — programozott diffel igazoltan — bit-pontosan változatlan maradt; ez, együtt a teljes 9.1–9.5 tesztkészlet módosítás nélküli, zöld futásával, empirikusan igazolja a backward compatibility-t `distortion=None` mellett. Az implementáció során Claude Code két, önként jelzett korrekciót végzett a jóváhagyott prompt saját tesztadat-hibáin (nem az implementáción): egy koordináta-eltolás értékét `1.0`-ról `0.37`-re változtatta (`wavelength=1.0` mellett egy teljes hullámhossznyi eltolás a szinusz periodicitása miatt észrevehetetlen lett volna), és egy távolság-próbapontot `10·radius`-ról `2·radius`-ra (mert `exp(-100)` a `pytest.approx` toleranciája alatt van) — mindkettőt a Szoftverarchitekt felülvizsgálta és helyesnek találta. A teljes `tests/plugins/relief_generator/` tesztkészlet 136/136 teszttel zöld, `ruff`/`mypy --strict --explicit-package-bases` hibamentes. **Ezzel a Phase 9 mind a hat domain-contractja (9.1–9.6) implementálva és review-n átment.** A hátralévő munka a 9.7 (Integration, validation, documentation): a `WaveGenerator`/`WaveParameters` tényleges kiterjesztése (a 9.2–9.6 domain-építőelemek bekötése), a `ParameterSpec`/ADR-0017 skalár-only korlátjának kezelése (a 9.2 lezárásakor egyeztetett nyitott kérdés), end-to-end integrációs teszt és élő tesztelés a projektgazdával.
>
> **Megjegyzés (2026-08-21, folytatás 38):** A 9.7.a (`ParameterSpec` bővítése lista-típussal) implementációja elkészült és review-n átment — ez volt a Phase 9 első lépése, amely a SliceDesigner core kódját módosította. A projektgazda a "Teljes bekötés" opciót választotta a 9.7 hatókörére (minden Phase 9 képesség GUI-konfigurálható lesz). A `ParameterType` egy ötödik, `"list"` értékkel bővült, a `ParameterSpec` egy új `item_schema: tuple[ParameterSpec, ...]` mezőt kapott (kizárólag skalár elemek, nincs beágyazott lista — ugyanaz az elv, mint a `WaveSet`-nél). A `docs/adr/0017-plugin-discovery-and-parameter-schema.md` kiegészítéssel bővült (nem új ADR-ként — az ADR saját "Következmények" szakasza ezt már előre jelezte). A core GUI-ban (`src/slicedesigner/gui/parameter_panel.py`) új `_ListParameterWidget` osztály jött létre: soronként egy, az `item_schema` alapján generikusan épített, a meglévő `_GeneratorParameterForm`-ot rekurzívan újrafelhasználó mini-form, Hozzáadás/Eltávolítás gombokkal. **Ez a lépés kizárólag magát a mechanizmust bővítette — a relief_generator plugin Phase 9 paramétereit (Radial envelope/propagation, explicit forráslista, Distortion) még nem köti be**; ez a 9.7.b–d al-lépések feladata. A `tests/project/`+`tests/gui/` tesztkészlet 199 teszttel zöld, a teljes Phase 9 plugin-tesztkészlet (136 teszt) érintetlen és zöld, `ruff`/`mypy --strict` hibamentes. A review során talált, ehhez a lépéshez nem tartozó, előzetesen is fennálló hiba: a `tests/gui/test_main_window_mesh_generation.py::test_generate_and_run_buttons_disabled_while_generating` a helyi tesztkörnyezetben `ModuleNotFoundError: No module named 'plugins.relief_generator.source.registration'` hibával elbukik entry point betöltéskor — `git stash`-sel igazoltan tiszta `main`-en is reprodukálódik, környezeti/csomagolási jellegű, nem ennek a lépésnek a hibája; nyitva marad, később megvizsgálandó. A hátralévő munka: 9.7.b (`WaveParameters` bővítése), 9.7.c (`WaveGenerator` bekötése a domain-rétegre), 9.7.d (`registration.py` `_PARAMETERS` bővítése), 9.7.e (end-to-end integrációs teszt), 9.7.f (élő tesztelés a projektgazdával).
>
> **Megjegyzés (2026-08-21, folytatás 39):** A 9.7.b (`WaveParameters` bővítése) implementációja elkészült és review-n átment. A `plugins/relief_generator/domain/wave_parameters.py` három opcionális mezővel bővült: `envelope: AmplitudeEnvelope | None = None`, `distortion: Distortion | None = None`, `sources: tuple[WaveSourceSpec, ...] = ()`. Az `envelope`/`distortion`, ha meg vannak adva, egységesen az összes automatikusan generált komponensre alkalmazódnak; a `sources` az automatikusan generált komponensek UTÁN kerül a végső `WaveSet`-be (`MULTIPLE_WAVE_SOURCES.md` 5. szakasz sorrendje). Mindhárom mező alapértékkel rendelkezik, nincs új kivétel (az egyes elemek — `RadialAmplitudeEnvelope`, `SwirlDistortion`, `WaveSourceSpec` — már önmagukat validálják). A lépés — a 9.2–9.5 mintáját folytatva — tisztán additív volt: a `WaveGenerator` egyetlen sora sem változott, még nem olvassa az új mezőket. A `tests/plugins/relief_generator/domain/test_wave_parameters.py` kivételesen bővült (import-blokk kiegészítés + hat új teszt a fájl végéhez fűzve), a meglévő három teszt és a `_VALID_KWARGS` változatlan maradt. A teljes `tests/plugins/relief_generator/` tesztkészlet 142/142 teszttel zöld, `ruff`/`mypy --strict --explicit-package-bases` hibamentes. A hátralévő munka: 9.7.c (`WaveGenerator` bekötése a domain-rétegre — az új mezők tényleges felhasználása), 9.7.d (`registration.py` `_PARAMETERS` bővítése), 9.7.e (end-to-end integrációs teszt), 9.7.f (élő tesztelés a projektgazdával).
>
> **Megjegyzés (2026-08-21, folytatás 40):** A 9.7.c (`WaveGenerator` bekötése a domain-rétegre) implementációja elkészült és review-n átment — ez volt a második lépés 9.1 óta (a 9.6 után), amely magát a `wave_generator.py`-t módosította. A `_build_wave_set` immár ténylegesen felhasználja a `WaveParameters.envelope`/`distortion`/`sources` mezőit: minden automatikusan generált komponens envelope-ja `parameters.envelope` (ha meg van adva, egységesen), különben `UniformEnvelope()`; a `distortion`-je `parameters.distortion` (lehet `None`); a végső `WaveSet` a `build_combined_wave_set(automatikus_waves, parameters.sources)`-ból származik a korábbi közvetlen `WaveSet(...)` konstrukció helyett. Egy szándékos, viselkedés-megőrző apró egyszerűsítés is történt: egyetlen megosztott `envelope`-példány a ciklus minden iterációjában újra létrehozott példány helyett (a frozen dataclass-ok immutabilitása miatt viselkedésileg semleges). A komponens-előállítási képletek, a `_sample_extrema`, a `_rho` és a `generate()` törzse szó szerint változatlan. A `tests/plugins/relief_generator/generators/test_wave_generator.py` kivételesen bővült: a fájl végéhez négy új teszt került (envelope hatása, distortion hatása, sources hozzáadása, determinizmus mindhárommal együtt), a meglévő három teszt — programozott diffel igazoltan — bit-pontosan változatlan maradt és továbbra is hibátlanul lefut, ami empirikusan igazolja a backward compatibility-t `envelope=None`/`distortion=None`/`sources=()` mellett. A teljes `tests/plugins/relief_generator/` tesztkészlet 146/146 teszttel zöld, `ruff`/`mypy --strict --explicit-package-bases` hibamentes. A hátralévő munka: 9.7.d (`registration.py` `_PARAMETERS` bővítése — a relief_generator plugin ténylegesen GUI-n keresztül elérhetővé teszi ezeket a képességeket), 9.7.e (end-to-end integrációs teszt), 9.7.f (élő tesztelés a projektgazdával).
>
> **Megjegyzés (2026-08-21, folytatás 41):** A 9.7.d (`registration.py` `_PARAMETERS` bővítése) implementációja elkészült és review-n átment. Az első 11, Phase 8-as `ParameterSpec` változatlan maradt, mögé 12 új elem került: `envelope_type`/`distortion_type` enum ("None" + a konkrét típus), a hozzájuk tartozó, mindig látható skalár mezők (center X/Y, radius, falloff-választó, sharpness, illetve center X/Y, radius, strength), valamint egy `sources` lista-mező (9.7.a `list` típus), 8 mezős `item_schema`-val (`source_type`, `amplitude`, `wavelength`, `phase`, `weight`, `direction`, `source_x`, `source_y`). Három új privát segédfüggvény (`_build_envelope`, `_build_distortion`, `_build_sources`) fordítja le a lapos `values` dict-et a domain-objektumokra — a `_build_sources` a `source_type` alapján kizárólag a releváns mezőket adja át a `WaveSourceSpec`-nek, a másik típus mezőit kihagyva (a `WaveSourceSpec` kölcsönös kizárása miatt). **Elismert, dokumentált korlát:** minden mező mindig látható a GUI-n, típustól függetlenül — a jelenlegi `ParameterSpec` mechanizmus nem támogat feltételes mezőláthatóságot; ha ez a gyakorlatban zavaróvá válik, egy jövőbeli backlog-tétel tárgya lehet. A teljes `tests/plugins/relief_generator/` tesztkészlet 155/155 teszttel zöld, `ruff` hibamentes; a `mypy --strict` egy előzetesen is fennálló, nem e lépéshez tartozó `import-untyped` figyelmeztetést jelzett a `slicedesigner.project.mesh_source_registry` importra (bizonyítottan nem regresszió). **Ezzel a relief_generator plugin mind a hat Phase 9 domain-építőeleme ténylegesen GUI-n keresztül is konfigurálhatóvá vált.** A hátralévő munka: 9.7.e (end-to-end integrációs teszt a SliceDesigner pipeline-nal), 9.7.f (élő tesztelés a projektgazdával).
>
> **Megjegyzés (2026-08-21, folytatás 42):** A 9.7.e (end-to-end integrációs teszt) implementációja elkészült és review-n átment. Az új `tests/plugins/relief_generator/source/test_phase9_end_to_end.py` egy kevert Phase 9-konfigurációval (automatikus Directional komponensek, explicit Radial és Directional forrás, Radial envelope, Swirl distortion — mind a hat Phase 9 domain-építőelem legalább egyszer szerepel) igazolja a teljes downstream láncot: `WaveGenerator → HeightField → ReliefGeometry → MeshGenerator → Mesh → ReliefGeneratorMeshSource.get_mesh()`, majd a Mesh-t a core, változatlan `slicedesigner.engines.slice_engine.create_slice_set()`-tel is sikeresen szeleteli — ez bizonyítja, hogy a Phase 9 bővítés nem igényel változtatást a downstream pipeline-ban. A `slice_thickness_mm`-et a teszt a ténylegesen létrejött Mesh Z-kiterjedéséből vezeti le (nem egy előre feltételezett `base_thickness+relief_height` értékből), hogy a `create_slice_set()` 2%-os skálázási tűrése semmilyen körülmények között ne akadályozza a szeletelést. A teljes `tests/plugins/relief_generator/` tesztkészlet 159/159 teszttel zöld, `ruff` hibamentes. A review során talált, ehhez a lépéshez nem tartozó, előzetesen is fennálló probléma: a `tests/gui/` egy Qt-threading tesztje jelenleg akadályozza a teljes core tesztkészlet együttes futtatását ebben a környezetben — Claude Code igazolta, hogy ez a repó egyéb, még nem commitolt módosításaitól függetlenül is fennáll, nem regresszió. **Ezzel a Phase 9 (Wave Extension) teljes automatizált munkája — mind a hat domain-contract implementációja és a teljes Integration — lezárult.** Az egyetlen hátralévő tétel a 9.7.f: élő tesztelés a projektgazdával, aminek eredményét dokumentálva zárható le véglegesen a 9.7 és ezzel a teljes Phase 9.
>
> **Megjegyzés (2026-08-21/23, folytatás 43):** A 9.7.f (élő tesztelés) első köre lezajlott, és négy tételt eredményezett. **(1) Súlyozási "hiba" tisztázva:** a projektgazda saját vizsgálata nem volt alapos; a domain-réteg és a GUI-widget-kód valódi Qt-futtatással igazoltan helyesen kezeli a több, eltérő súlyú explicit forrást — nincs kódváltoztatás. **(2) Megosztott envelope/distortion + `include_automatic` (a `registration.py` GUI-bővítéssel együtt):** a projektgazda felülbírálta a `MULTIPLE_WAVE_SOURCES.md` két korábbi korlátozását — az explicit forrásokra mostantól hat a `WaveParameters.envelope`/`distortion`, és a `WaveParameters` egy új `include_automatic: bool = True` mezővel az automatikus generálás teljesen kikapcsolható (`WaveSet` üresség esetén a meglévő `WaveSetValueError` fail-fast érvényesül). Ehhez tartozott: `MULTIPLE_WAVE_SOURCES.md` 4./7. szakaszának korrekciója és új 9. szakasza; `multiple_wave_sources.py` (`build_wave`/`build_combined_wave_set` opcionális `envelope`/`distortion` paraméterrel) és `wave_generator.py` (`include_automatic`-ág, megosztott envelope/distortion átadása az explicit forrásoknak is) bővítése; és a `registration.py`-ban egy új `include_automatic` `ParameterSpec` ("Igen"/"Nem" enum, mivel a `ParameterSpec`-nek nincs `bool` típusa). A teljes `tests/plugins/relief_generator/` tesztkészlet 170/170 teszttel zöld. **(3) Backlog-tétel:** az explicit hullámforrások `irregularity`/`complexity` paraméterének hiánya (koncentrikus rétegzés mint preferált geometriai interpretáció) `docs/BACKLOG.md` 6. tételként rögzítve — nem aktív munka. **(4) 3D előnézet — fényforrás-hiba javítva:** a nyers Mesh nézet vizuálisan lapos maradt finom mintázatoknál. Egy első, hibás diagnózis (renderelési háttér-korlátozás feltételezése) után a projektgazda közvetlen vizsgálattal megtalálta a valódi okot: a PyVista `Plotter.clear()` a jelenet összes fényforrását törli, amit semmi nem állított vissza, ezért a rendszer már az első rendereléstől kezdve fényforrás (és így bármilyen irányfüggő árnyékolás) nélkül futott — ez teljesen független volt a GPU-tól/drivertől. Javítás: új `_clear_plotter()` helper (`clear()` + `enable_lightkit()`) mindkét renderelési útvonalon (`_render_mesh`, `_render_geometry_bundle`); az `_add_solid_with_edges()` `smooth_shading` paramétere opt-in lett (alapértelmezetten `False`) — kizárólag az "Eredeti Mesh" nézet (`_render_mesh()`) kéri explicit `True`-val, a "Szeletelt összeállítás" nézet (fizikailag lapos, rétegzett geometria, ahol a Phong-simítás hamis fénycsíkokat keltene a rétegek határán) nem. A `tests/gui/test_preview_panel.py` a korábbi összevont tesztet két, célzott tesztre bontotta (`test_original_mesh_uses_smooth_shading_and_specular`/`test_sliced_assembly_does_not_use_smooth_shading`); mind a 31 teszt zöld. **Ez a lépés kivételesen a szokásos Döntési javaslat → Hatásvizsgálat → jóváhagyás → prompt folyamaton kívül, közvetlen élő hibakereséssel készült** — a Szoftverarchitekt utólag, review után fogadta el, a dokumentációs nyomvonal (ez a bejegyzés) pótlásával. A Szoftverarchitekt egy korábbi, magasság-alapú színezési workaround-javaslata (amely a tünetet, nem az okot kezelte volna) emiatt nem került bevezetésre. A hátralévő munka: a 9.7.f élő tesztelés folytatása/lezárása, majd a 9.7 fő ROADMAP-sor és a teljes Phase 9 véglegesítése.
>
> **Megjegyzés (2026-08-23, folytatás 44):** A 9.7.f (élő tesztelés) lezárult, a projektgazda megerősítette, hogy nincs több nyitott kérdés. A Phase 9 (Wave Extension) mind a hét tétele (9.1–9.7) elkészült, review-n átment; a hat domain-contract dokumentum (9.1–9.6) már korábban Elfogadva státusszal a végleges `docs/plugins/relief_generator/` alá került; az implementáció automatizált teszttel lefedve és a projektgazda többkörös élő tesztelésével megerősítve. A Kilépési feltétel teljesült. A Phase 9 ezért ✅ Approved-ra került. A projektgazda kérdésére tisztázva: a `docs/BACKLOG.md` tételei (a Phase 9 során felvett 5. és 6. tétellel együtt) a `BACKLOG.md` saját definíciója szerint kifejezetten nem tartoznak egyetlen fázis kilépési feltételei közé sem — ezek számbavétele és egy esetleges következő fázis megnyitása a Phase 9 lezárása UTÁNI, külön döntés tárgya (Általános szabályok: "Egyszerre csak egy fázis lehet aktív. A következő fázis csak az előző lezárása után kezdhető meg."). A ROADMAP-ban emiatt egyelőre nem nyílik meg új fázis.
>
> **Megjegyzés (2026-08-23, folytatás 45):** A projektgazdával lezajlott tervezési egyeztetés nyomán megnyílt a Phase 10 (Hullámforma-variánsok és eljárásos zajmoduláció) — a `docs/BACKLOG.md` öt tételéből (1, 3, 4, 5, 6) építkezik. A sorrend indoklása: a 10.1 azért kerül előre, hogy a 10.2/10.3 ne zsúfolja tovább a már most is zsúfolt formot; a 10.2 kifejezetten kiterjed a `WaveSourceSpec`-re is, nem csak az automatikus generálásra, mert enélkül a 10.3 értéktelen lenne; a 10.3 a 10.2 `WaveSourceSpec`-bővítésétől függ; a 10.4 új tétel, a 10.5 és a 10.6 közös eljárásos zajmező-alapja, mivel a `BACKLOG.md` 3. és 4. tétele valójában egy fejlesztési kör két rátétje, közös alapkövet igényel. A `BACKLOG.md` 2. tétele (Wave Generatortól független Height Field generátor-típusok, pl. Voronoi/dűne) a projektgazda kifejezett döntése alapján **nem** kerül be Phase 10-be — önálló, jövőbeli fázis marad, egyelőre megnyitás nélkül, a Phase 8 lezárásakor alkalmazott mintát követve. A `BACKLOG.md` öt beemelt tétele emiatt törlésre került a Backlogból; a megmaradt (Height Field generátor-típusok) tétel 1. sorszámra került át.
>
> **Megjegyzés (2026-08-23, folytatás 46):** A 10.1 tétel (`ParameterSpec`/GUI-form vizuális csoportosítása) elkészült, review-n átment: a `ParameterSpec` új `group: str | None = None` mezővel bővült (ADR-0017 kiegészítés), a `_GeneratorParameterForm` az azonos csoportú mezőket egy közös, alapból csukott `_CollapsibleSection`-be rendezi; a relief_generator `_PARAMETERS`-e három csoportot kapott ("Automatikus hullám", "Envelope", "Torzítás"), az alapgeometria és a `sources` lista csoport nélkül maradt. A megbeszélés során felmerült, korábban nem tervezett kiegészítésként a "Generálás" gomb is fix footerré vált (`ParameterPanel._add_tab()` új `footer` paraméter) — a görgethető formtól függetlenül, mindig látható. A teljes tesztkészlet (520 teszt) regresszió nélkül zöld, `ruff`/`mypy` hibamentes (egy pre-existing, nem e lépéshez tartozó `mypy --strict` figyelmeztetés kivételével). A hátralévő munka a 10.2 (alternatív `WaveFunction`-ök, kiterjesztve a `WaveSourceSpec`-re is).
>
> **Megjegyzés (2026-08-23, folytatás 47):** A 10.2 tétel (alternatív `WaveFunction`-ök) elkészült, review-n átment: a Cosine helyett — mivel matematikailag megegyezik egy fázis-eltolt Sinusoidal-lal — a klasszikus Sine/Triangle/Sawtooth/Square négyes valósult meg. `Triangle`/`Sawtooth`/`Square` új `WaveFunction`-ök (`wave.py`), `WaveParameters.function` (egységes, minden automatikus komponensre) és `WaveSourceSpec.function` (forrásonként egyedi) mező, mindkettő `"Sinusoidal"` alapértékkel — backward compatible. GUI-bekötés a 10.1 csoportosítására építve (`function` az "Automatikus hullám" csoportban, plusz egy csoport nélküli `function` mező a `sources` listában). A `WAVE_DOMAIN_MODEL.md` 6.3/16.1 és a `MULTIPLE_WAVE_SOURCES.md` 4. szakasza frissült. Teljes tesztkészlet (564 teszt) regresszió nélkül zöld. A hátralévő munka a 10.3 (explicit hullámforrások `irregularity`/`complexity`-je, koncentrikus rétegzéssel).
>
> **Megjegyzés (2026-08-23, folytatás 48):** A 10.3 tétel (explicit hullámforrások `irregularity`/`complexity`-je, koncentrikus rétegzéssel) elkészült, review-n átment: a `MULTIPLE_WAVE_SOURCES.md` korábbi, kifejezett korlátozása feloldva. Új, megosztott domain-modul (`deterministic_components.py`) vonja össze a `WAVE_FUNCTION_MODEL.md` 22. szakaszának determinisztikus komponens-szabályát, amit mostantól a `WaveGenerator` (automatikus generálás) ÉS a `multiple_wave_sources.build_waves()` (explicit forrás, ROADMAP Phase 10.3) is használ. A `WaveSourceSpec` új `irregularity`/`complexity` mezőt kapott (mindkettő `0.0` alapértékkel — backward compatible); `build_wave()` átnevezve `build_waves() -> tuple[Wave, ...]`-re, koncentrikus rétegzéssel (azonos `direction`/pozíció minden rétegen, csak amplitúdó/hullámhossz/fázis csökken/jitterel rétegenként). GUI-bekötés a `sources` listában, csoport nélkül. Teljes tesztkészlet (591 teszt) regresszió nélkül zöld. A hátralévő munka a 10.4 (megosztott eljárásos zajmező-primitív, a 10.5/10.6 közös alapja).
>
> **Megjegyzés (2026-08-23, folytatás 49):** A 10.4 tétel (megosztott eljárásos zajmező-primitívek) elkészült, review-n átment. A projektgazdával lezajlott tervezési egyeztetés nyomán a tétel a `BACKLOG.md` 3./4. tételéből eredő, eredeti tervnél gazdagabban valósult meg: két önálló, determinisztikus primitívum — `GradientNoiseField` (Perlin-szerű, gradiens-alapú, `[-1,1]`, opcionális `octaves` fraktál-kombinációval) és `VoronoiNoiseField` (cellás/Worley F1-távolság, `[0,1]`) —, mindkettő tisztán aritmetikai, `random` modul nélkül. A megbeszélés során felmerült, hogy a `BACKLOG.md` 1. (megmaradt) tétele (recept-generátorok: dűne, holdkráter, faerezet) erre a gazdagabb primitívum-készletre épülhetne — a projektgazda döntése alapján ez **nem** része a Phase 10-nek, továbbra is önálló, jövőbeli fázis marad, de a `GradientNoiseField`/`VoronoiNoiseField` immár készen áll az alapjául. Nincs GUI-kitettség, nincs `AmplitudeEnvelope`/`Distortion`-becsomagolás — ez a 10.5/10.6 hatóköre. Új domain-contract dokumentum: `PROCEDURAL_NOISE.md`. Teljes tesztkészlet (274 teszt a relief_generator alatt) regresszió nélkül zöld. A hátralévő munka a 10.5 (amplitude distortion mint `AmplitudeEnvelope`-bővítés, a 10.4-re építve).
>
> **Megjegyzés (2026-08-23, folytatás 50):** A 10.5 tétel (`NoiseAmplitudeEnvelope`) elkészült, review-n átment: a 10.4-es `GradientNoiseField`/`VoronoiNoiseField` első tényleges becsomagolása egy apró, helyben (`amplitude_envelope.py`) definiált `NoiseSource` Protocol-lal (`sample(x,y)->float`), amit mindkét primitívum módosítás nélkül kielégít. Az új `NoiseAmplitudeEnvelope` egy `[input_min,input_max]` tartományból lineárisan `[0,1]`-re képez. GUI-bekötés: az `envelope_type` harmadik választása `"Noise"`, egy belső `envelope_noise_type` (Gradient/Voronoi) almezővel, hat új "Envelope" csoportos paraméterrel — ez az első Phase 10.4-utáni tétel, ami ténylegesen elérhető GUI-n keresztül is. Tanulság: a `AMPLITUDE_ENVELOPE.md` már tartalmazott egy, a promptból nem ismert 10. szakaszt ("Envelope által létrehozott konstans nulla eredmény") — Claude Code helyesen megállt és jelezte az eltérést a szó szerinti egyezés megkövetelése miatt, ahelyett hogy vakon duplikált volna egy "## 10." fejlécet; a projektgazda döntése alapján az új tartalom 11/12-re számozódott, majd egy külön, célzott prompt javította a három emiatt elavulttá vált kódbeli kereszthivatkozást (`amplitude_envelope.py` 2×, `exceptions.py` 1×). Teljes tesztkészlet (286 teszt) regresszió nélkül zöld. A hátralévő munka a 10.6 (további `Distortion`-típusok, a 10.4-re építve) — ezzel zárul a Phase 10.
>
> **Megjegyzés (2026-08-23, folytatás 51):** A 10.6 tétel (`NoiseDistortion`) elkészült, review-n átment — ezzel mind a hat Phase 10 tétel (10.1–10.6) kész. A `NoiseDistortion` a `Distortion` Protocol harmadik megvalósítása, kizárólag `GradientNoiseField`-et használva (nem `VoronoiNoiseField`-et — koordináta-eltoláshoz előjeles `[-1,1]` érték kell); klasszikus "domain warping": az X/Y eltolást két, dekorrelált (`seed`/`seed+1`) `GradientNoiseField` adja, helyben definiált `NoiseSource` Protocol-lal (nem import az `amplitude_envelope.py`-ból). A "több lépték kombinálása" a meglévő `octaves` paraméteren keresztül ingyen megvolt. Ezúttal a `PROCEDURAL_DISTORTION.md` a promptban feltételezett 8. szakasznál ténylegesen véget is ért — a 10.5-nél tanult óvatosság (explicit ellenőrzés beszúrás előtt) ezúttal nem jelzett eltérést, de a védőháló megmaradt. A `PROCEDURAL_DISTORTION.md` 7. szakaszának mindkét, korábban "Hatókörön kívül" jelzett pontja (amplitude distortion, további Distortion-típusok) "megvalósítva"-ra jelölve. A Phase 10 Kilépési feltétele teljesült — mind a hat tétel elkészült, a szükséges domain-contract dokumentumok (ADR-0017 kiegészítés, WAVE_DOMAIN_MODEL.md, MULTIPLE_WAVE_SOURCES.md, PROCEDURAL_NOISE.md, AMPLITUDE_ENVELOPE.md, PROCEDURAL_DISTORTION.md) Elfogadva a végleges helyükön, az implementáció automatizált teszttel lefedve (292 teszt a relief_generator alatt, teljes repo regresszió nélkül). A Phase 10 ezért ✅ Approved-ra került. A ROADMAP-ban emiatt egyelőre nem nyílik meg új fázis — a `BACKLOG.md`-ben megmaradt 1. tétel (Wave Generatortól független Height Field generátor-típusok) továbbra is önálló, jövőbeli fázis, egyelőre megnyitás nélkül.

## Állapotjelölések

| Jelölés | Jelentés |
|---|---|
| ⬜ Not Started | A fázis még nem kezdődött el. |
| 🟡 In Progress | A fázis folyamatban van. |
| 🟢 Review | A fázis eredménye elkészült, felülvizsgálat alatt áll. |
| ✅ Approved | A fázis eredményét a projektgazda jóváhagyta. |
| 🔒 Locked | A fázis lezárva; kizárólag ADR alapján módosítható. |

## Fázisok

### Phase 0 – Project Foundation

Állapot: ✅ Approved

Feladata:

* projektstruktúra
* README
* PROJECT_CONSTITUTION
* PROJECT_VISION
* ENGINEERING_PRINCIPLES
* ARCHITECTURE
* PROJECT_STRUCTURE
* CODING_STANDARDS
* AI_WORKFLOW
* PROMPT_STANDARD

Kilépési feltétel: A projekt dokumentációja önmagában értelmezhető.

---

### Phase 1 – Domain Design

Állapot: ✅ Approved

Feladata:

* DOMAIN_MODEL (aktív dokumentum)
* koordinátarendszer
* mértékegységek
* alapvető objektummodell
* anyagmodell
* szeletmodell
* összeállítási modell

Kilépési feltétel: A projekt egységes fogalomrendszerrel rendelkezik.

---

### Phase 2 – Functional Specifications

Állapot: ✅ Approved

Minden fő funkció külön specifikációban készül, például:

* Mesh Import
* Slice Engine
* Gap System
* Dowel System
* Backplate
* Numbering
* Nesting
* DXF Export

Kilépési feltétel: Minden fő funkció rendelkezik jóváhagyott specifikációval.

---

### Phase 3 – Architecture Freeze

Állapot: ✅ Approved

Feladata:

* dokumentáció review
* architektúra véglegesítése
* hiányosságok javítása

Új funkció nem kerülhet be.

Kilépési feltétel: Az architektúra Approved állapotú.

---

### Phase 4 – Implementation

Állapot: ✅ Approved

A fejlesztés kizárólag jóváhagyott specifikáció alapján történt. Mind a nyolc engine (Mesh Import, Slice Engine, Dowel System, Gap System, Backplate, Numbering, Nesting, DXF Export) elkészült, automatizált teszttel (89 teszt) lefedve.

---

### Phase 5 – Integration

Állapot: ✅ Approved

Feladata:

* modulok összekapcsolása
* GUI
* teljes workflow
* projektmentés
* beállítások

Megjegyzés: az eredeti öt tételen felül a GUI-t lezáró négy kiegészítő tétel (automatikus Mesh-előnézet, Dowel/Spacer/Backplate 3D-megjelenítés, szeletenkénti kiemelés) és két, élő teszteléskor feltárt javítás (Dowel automatikus pozíciókeresés térbeli szórása, Numbering figyelmeztetés-alapú kezelése) is a fázis részeként valósult meg.

---

### Phase 6 – Release Candidate

Állapot: ✅ Approved

Feladata:

* ~~DXF export leválasztása a Futtatásról (önálló interakció)~~ — kész (ADR-0009)
* ~~optimalizálás — Dowel automatikus pozíciókeresés teljesítmény-javítása (1. kör)~~ — kész
* ~~optimalizálás — 2. kör (Numbering, Dowel, GUI-előnézet szálkezelés)~~ — kész, ld. lentebb
* ~~végső tesztelés~~ — kész, ld. lentebb
* ~~dokumentáció~~ — kész:
  * ~~6.1 – Felhasználói dokumentáció (`docs/USER_GUIDE.md`): telepítés és indítás; első projekt létrehozása; STL importálása; szeletelési beállítások használata; Gap / Dowel / Backplate / Numbering / Nesting használata; DXF export; projekt mentése és visszatöltése; beállítások használata; gyakori hibák / problémák.~~ — kész
  * ~~6.2 – Workflow dokumentáció (`docs/WORKFLOW.md`): a teljes ajánlott munkafolyamat az importtól az exportig; a funkciók javasolt használati sorrendje; milyen bemeneti modellből milyen kimenet várható; CNC gyártási szempontból fontos korlátozások.~~ — kész
  * ~~6.3 – Technikai dokumentáció véglegesítése: README, ARCHITECTURE, PROJECT_STRUCTURE aktualizálása; specifikációk és a tényleges implementáció összevetése; elavult vagy ellentmondó dokumentáció megszüntetése; dokumentációs hivatkozások ellenőrzése.~~ — kész (audit 6 elavult hivatkozást/dátumot talált és javított: README ADR-tartomány, ARCHITECTURE.md 3 hiányzó ADR-hivatkozás, PROJECT_STRUCTURE.md hiányzó fájlok, 3 specifikáció elavult "Utolsó módosítás" dátuma)
  * ~~6.4 – Release dokumentáció (`docs/RELEASE_NOTES.md`): verziószám; támogatott platformok; rendszerkövetelmények; ismert korlátozások; ismert hibák; release notes / changelog.~~ — kész
* ~~példaprojektek~~ — kész:
  * ~~6.5 – Alap példaprojekt (`examples/basic_example/`): egyszerű 3D modell, minimális szeletelési beállítások, szeletelt eredmény — az alap workflow bemutatására.~~ — kész
  * ~~6.6 – Összetett példaprojekt (`examples/complex_example/`): több szelet, Gap, Dowel, Numbering, Backplate, export — a fő funkciók együttműködésének bemutatására.~~ — kész
  * ~~6.7 – Nesting példaprojekt (`examples/nesting_example/`): több alkatrész, Nesting használata, gyártási elrendezés, exportált eredmény.~~ — kész
  * ~~6.8 – Teljes referencia projekt (`examples/reference_project/`): reális, összetettebb modell, teljes beállításkészlet, projektfájl, bemeneti modell, exportált eredmények, rövid leírás a beállítások indoklásáról.~~ — kész
  * ~~6.9 – Példaprojektek validálása: minden példa reprodukálható; a projektfájlok ténylegesen betölthetők; az eredmények megfelelnek a dokumentált elvárásoknak; egyetlen példa sem működik kizárólag valamilyen kézzel elvégzett, dokumentálatlan lépés miatt.~~ — kész

Kilépési feltétel: egy új fejlesztő vagy felhasználó a dokumentáció alapján képes a program telepítésére, használatára és a teljes workflow megértésére, miközben a dokumentáció és a tényleges rendszer nincs ellentmondásban; az `examples/` tartalma önállóan demonstrálja a Slice Designer fő képességeit, és minden példaprojekt reprodukálható. **✅ Teljesült.**

#### Végső tesztelés — 2026-08-07-ig lezárt tételek

Egyetlen, hosszú munkamenetben, valódi felhasználói modelleken (elsősorban "Wobbly Toad" és "face-in-the-brick-wall" STL) végzett élő tesztelés az alábbi, ténylegesen kijavított és Claude Code által végrehajtott hibákat tárta fel:

**Slice Engine**

* X/Y tengelyű szeletelésnél a `trimesh` belső, dokumentálatlan vetítési konvenciója (mátrix-elemzéssel és két független, aszimmetrikus doboz-teszttel igazoltan) eltért attól, amit a kód (`_SLICE_AXIS_CONTOUR_ORDER`) feltételezett — ez egy önmagában, függetlenül bizonyított és javított hiba, explicit, kézzel megkonstruált `_TO_2D_ROTATION` mátrixokkal.
* **Fontos, pontosan rögzítendő korlát:** ez a javítás **nem** oldotta meg a projektgazda eredeti, élő tesztelésnél jelentett panaszát, hogy a DXF-en a szelet-kontúr tükrözve jelenik meg — a panasz a javítás után is fennállt. Az akkori magyarázat (a Szoftverarchitekt részéről: "a lap másik oldaláról nézed") **soha nem lett bizonyítva** — a projektgazda csak elfogadta, mert akkor nem tartotta elég fontosnak a további vitát, nem azért, mert megalapozottnak találta. Ez tehát **nem lezárt tétel**, ld. a NYITOTT szakaszt.

**Numbering Engine (szelet-oldal)**

* `_glyph_to_local()`: karakter-szintű tükröződés bizonyos tengely/előjel-kombinációknál — javítva egy, az indextől/előjeltől teljesen független, vizuálisan ellenőrzött újratervezéssel.
* `_search_best_anchor()`: a fenti javítás után a keresés gyors útja gyakorlatilag mindig a teljes rácsos bejárásra esett vissza — visszaállítva a teljesítmény négy gyors sarok-jelölt próbájával (~186×-os `_fits()`-hívás csökkenés egy valós teszt-fixture-nél).

**Gap Engine**

* `region_id` a teljes Slice Set-en át folyamatosan növekedett, nem Gap-enként — a hibaüzenetek megtalálhatatlan régiókra mutattak; javítva Gap-enkénti újraindítással.
* `min_spacers_per_region` csak `≥1` lehetett, ezért egy apró, fizikailag Spacer-re alkalmatlan régió leállította a teljes feldolgozást — bővítve `0`-ra (explicit, tudatos beállítás, figyelmeztetéssel).

**Backplate Engine — a legnagyobb terjedelmű téma**

* A közös Backplate-sík felismerése régen egyetlen kilógó szigetet is hibaként kezelt — lecserélve automatikus, lánc-alapú klaszterezésre (domináns csoport + szigorú többségi biztonsági feltétel); a klaszterezésen kívül eső szigetek automatikusan, figyelmeztetéssel kizárásra kerülnek.
* `usable_length ≤ 0` egy érintkező szakaszon szintén hiba volt — figyelmeztetéssé enyhítve (a sziget más szakaszain/más szigeteken a feldolgozás folytatódik).
* A `Backplate` objektum nem tárolta a közös sík world-koordinátáját — ez okozta a 3D előnézet hibás pozícióját (a klaszterezés bevezetése után egy korábbi, közelítő GUI-számítás elavulttá vált). Megoldás: új `Backplate.common_plane_mm` mező, a GUI a közelítés helyett közvetlenül ezt olvassa.
* A Backplate alakja eredetileg a teljes 3D modell nyers Mesh-ből vetített sziluettje volt — ez (a) fizikailag túl nagy volt (a teljes test árnyéka, nem a tényleges érintkezési terület), és (b) a nyers, újra nem bázisolt Mesh-koordinátákat használta, ami inkonzisztens volt a pipeline többi részének `position_mm`-alapú rendszerével (ez okozta, hogy a Backplate az összeállítás mellett, rossz pozícióban jelent meg a DXF exportban). Két lépésben javítva: először a már meglévő, domináns-klaszterbeli érintkező szakaszokból épített téglalap-unióra (helyes pozíció, de lépcsőzetes alak), majd — a projektgazda kifejezett kérésére, mert a szeletelés is a modell tényleges geometriájából dolgozik — egy valódi, a Slice Engine-nel megegyezően skálázott Mesh térbeli Boole-metszetére (`manifold3d` új függőség, ADR-0006 kiegészítve). A metszetnek szükségszerűen egy tűrés-sávot kell használnia (nem nulla-vastagságú síkot), mert egy enyhén nem-sík felületnél (pl. kerekített talp) egy egzakt metszet szinte semmit nem fogna ki — ezt a valódi modellen empirikusan igazoltuk.

**Numbering Engine (Backplate-oldal)**

* `apply_numbering_to_backplate()` saját, önálló `axis_min`-számítást végzett a nyers Mesh-ből, ami a Backplate `position_mm`-alapú koordinátarendszerével nem volt kompatibilis (a Backplate-javítás előtt ez rejtve maradt, mert a Backplate saját, akkor még szintén hibás sziluettje véletlenül konzisztens volt vele) — javítva, `axis_min` elhagyásával.

#### Végső tesztelés — NYITOTT, folyamatban lévő tételek

**1. Validálatlan feltételezés a szelet-kontúr "nézőpont" magyarázatáról — újra megvizsgálandó, NEM tekinthető lezártnak.**

> **2026-08-08-i frissítés ("folytatás 11"):** a "nézőpont" magyarázat cáfolva — a valódi gyökérok a `_TO_2D_ROTATION` szándékos, de hibás "csak forgatás" tervezési elve volt (ld. ADR-0010). A javítás megtörtént; a projektgazdai élő tesztelés (a puszta vágási vonalon, feliratok nélkül) megerősítésére vár.

A `_TO_2D_ROTATION` javítása után a projektgazda élő tesztelésnél továbbra is tükrözöttnek jelezte a szelet-kontúrt a DXF-ben. A Szoftverarchitekt akkor azt állította, hogy ez nem hiba, hanem a lapos alkatrész másik oldaláról nézés természetes következménye — ezt a projektgazda **elfogadta, de kifejezetten nem megerősítésként**, hanem mert akkor nem tartotta elég fontosnak, hogy tovább vitatkozzon róla. **Ez az állítás soha nem lett független bizonyítékkal alátámasztva** (szemben pl. a `_TO_2D_ROTATION`-nal, amit mátrix-elemzés és empirikus teszt is igazolt). Mivel a Backplate-nél egy hasonló, "nézőpont kérdésének" beállított jelenség a végén egy valódi, javítatlan hibának (`_glyph_point_rect`) bizonyult, ez a korábbi magyarázat **gyanús, és újra, ezúttal bizonyítékkal (nem érveléssel) megvizsgálandó** — nem szabad lezártként kezelni, amíg ez meg nem történik.

**2. Numbering Engine — Backplate-feliratok karakter-szintű tükröződése.**

> **2026-08-08-i frissítés ("folytatás 11"):** javítva (ld. ADR-0010) — az alábbi tervezett javítás változatlan formában végrehajtásra került, és emellett egy **harmadik, korábban fel nem ismert előfordulás** is javításra került: `nesting_engine.py::_glyph_point_rect()` (seam-oldal) — ld. az alábbi "Rendszerszintű audit" bekezdéshez fűzött korrekciót. A projektgazdai élő tesztelés megerősítésére vár.

Élő tesztelés (a `face-in-the-brick-wall` modellen, mert az kellően aszimmetrikus/jellegzetes ahhoz, hogy a tükröződés egyértelműen megállapítható legyen) feltárta, hogy a Backplate-en elhelyezett azonosítók (pl. "2/A") karakter-szinten tükrözve jelennek meg — ugyanaz a hibaosztály, mint amit a szelet-oldali `_glyph_to_local()`-nál már javítottunk, de egy **külön, önálló, korábban sosem javított duplikátumban**: `numbering_engine.py::_glyph_point_rect()`.

**A gyökérok pontosan azonosítva, a javítás megtervezve és — egy Szoftverarchitekt-oldali, ideiglenes sandbox-másolaton — vizuálisan ellenőrizve (a hibás és javított verzió egymás mellett kirajzolva, valódi karakter-adatokkal), DE MÉG NEM lett átadva Claude Code-nak végrehajtásra.** Ez az azonnali következő lépés egy új beszélgetésben.

A hibás kód (`upright=True` ág):

```python
def _glyph_point_rect(gx, gy, upright, anchor_x, anchor_y):
    if upright:
        return (anchor_x + gx, anchor_y - gy)  # <- tükrözés, det=-1
    return (anchor_x + gy, anchor_y - gx)      # <- ez az ág helyes, det=+1
```

A javítás (ellenőrizve, det=+1, a karakterek helyesen olvashatók):

```python
def _glyph_point_rect(gx, gy, upright, anchor_x, anchor_y, height_mm):
    if upright:
        return (anchor_x + gx, anchor_y - height_mm + gy)
    return (anchor_x + gy, anchor_y - gx)
```

(A `height_mm` új paraméter, a hívó `_build_text_strokes_rect()`-ből egyenesen továbbadható, mivel az már megkapja.)

**Rendszerszintű audit is megtörtént** (nem csak ez az egy hely lett ellenőrizve): a teljes `src/slicedesigner/engines/` és `src/slicedesigner/gui/` alatt átnézve, kizárólag ez a két függvény (`_glyph_to_local`, `_glyph_point_rect`) tartalmazott "horgonypont + glyph-eltolás" jellegű, kézzel írt koordináta-számítást — minden más, koordinátát összeállító függvény vagy a már bizonyítottan helyes, közös `_AXIS_MAPPING`/`_TO_2D_ROTATION` konvenciót használja, vagy egyszerű, számítás nélküli index-hozzárendelést végez.
>
> **Korrekció (2026-08-08, "folytatás 11"):** ez az állítás TÉVES volt. Egy későbbi, célzott audit egy **harmadik, önálló előfordulást** is talált: `nesting_engine.py::_glyph_point_rect()` (a toldási/seam al-azonosítók rajzolásánál), amelynek `non-upright` ága szintén determináns -1 (tükrözött) volt. Ez azért maradhatott észrevétlen a fenti "rendszerszintű audit" során is, mert a meglévő tesztek csak a szöveg illeszkedését ("elfér-e a rendelkezésre álló területen"), nem a tükrözöttségét ellenőrizték — ez a hibaosztály tehát vizuális/geometriai ellenőrzés nélkül, pusztán kód-átvizsgálással nem volt megbízhatóan kiszűrhető. Javítva, ld. ADR-0010.

**Nyitva maradó kérdés, amit élő teszteléssel kell megerősíteni, miután a fenti javítás megtörtént:** a projektgazda a Backplate **kontúrját** (nem csak a feliratait) is tükrözöttnek jelezte. A Szoftverarchitekt minden elérhető ellenőrzése (szintetikus aszimmetrikus teszt-alakzat a tényleges vetítési képlettel; a teljes lánc valódi modellen történő összevetése a szeletek saját adataival) a kontúrt helyesnek mutatta — lehetséges, hogy a "tükrözöttnek tűnés" részben vagy egészben a fenti, ténylegesen tükrözött feliratból eredt. **Ezt a fenti javítás után, a feliratoktól függetlenül (pl. a puszta vágási vonalat nézve), élőben külön meg kell erősíteni.**

> **Élő megerősítés (2026-08-08):** A `render_geometry.py::_AXIS_MAPPING`/`_backplate_third_axis_sign` GUI-oldali szinkronizálása, majd a `_backplate_third_axis_sign()` levezetésében talált hibás nézőpont-feltevés javítása (a teljes visszatérési érték globális előjel-megfordítása, ld. ADR-0010 "Frissítés (2026-08-08)" szakasza) után a projektgazda mindhárom szeletelési tengelyen (X, Y, Z) elvégezte az élő tesztelést, és megerősítette: a szelet-kontúr, a Backplate-kontúr és a Backplate-felirat egyaránt helyesen, tükrözés nélkül jelenik meg — mind a 3D előnézetben, mind az exportált DXF-en. A fentebb (a Phase 6 "NYITOTT" szakasz 1. tételében) leírt, korábban validálatlan "nézőpont" magyarázat ezzel okafogyottá vált: nem azért zárul le a kérdés, mert az az érvelés helyesnek bizonyult volna, hanem mert a tényleges gyökérokot (a Slice Engine vetítéséből hiányzó, majd az ADR-0010-zel bevezetett szándékos tükrözés, és annak Backplate-re és a GUI-ra kiterjedő teljes utókövetése) azonosítottuk és javítottuk. A "Végső tesztelés" tétel e vonatkozásban — a teljes tükrözési hibaosztály (szelet-kontúr, Backplate-kontúr, Backplate-felirat) — lezárva; a Phase 6 egyéb, még hátralévő tételei (dokumentáció, példaprojektek) miatt maga a Phase 6 továbbra is 🟡 In Progress marad.

#### Végső tesztelés — 2026-08-09-ig lezárt kiegészítő tételek

* **ADR-0010 tükröződés-javítás élő tesztelése megerősítve** — a "folytatás 11" bejegyzésben leírt gyökérok-javítást a projektgazda élesben, fizikai modellel összevetve megerősítette. Ezzel a szelet-kontúr és a Backplate (kontúr és felirat) tükröződési hibaosztálya véglegesen lezárva.
* **Backplate-csapok külön alkatrészként jelentek meg a DXF exportban, a hozzájuk tartozó szelet helyett/mellett** — gyökérok: a `backplate_engine.py::_apply_tab_geometry()` a csap-téglalapot a sziget tényleges (a `backplate_plane_tolerance_mm` tűrésen belül ingadozó) pereméhez képest pontatlanul illesztette, ami `unary_union()` után `MultiPolygon`-t (két külön szigetet) eredményezett nem tökéletesen sík érintkezési határnál. Javítás: a csap belső élének garantált átfedéssel történő illesztése (`_TAB_OVERLAP_SAFETY_EPSILON_MM`, a Dowel Engine tolerancia-mintájára). 236/236 automatizált teszt, élő teszttel megerősítve.
* **A Spacer-korongok közepén nem volt furat a rajtuk átmenő Dowel számára** — ez dokumentációs hiány is volt, nem csak implementációs: a `GAP_SYSTEM_SPEC.md`/`DOMAIN_MODEL.md` nem tartalmazott furat-attribútumot a Spacer-en. Javítás: a Dowel-re fűzött Spacer-ek új, opcionális `dowel_diameter_mm` attribútumot kapnak (a rajtuk átmenő Dowel átmérőjével), amiből a Nesting Engine a korong furatát vágja; az önálló (nem Dowel-alapú) Spacer-ek tömörek maradnak. `GAP_SYSTEM_SPEC.md`, `DOMAIN_MODEL.md`, `NESTING_SPEC.md` kiegészítve. 240/240 automatizált teszt, élő teszttel megerősítve.

#### Optimalizálás — 2. kör (2026-08-09)

Élő tesztelés közben jelzett, jelentős futásidő-panasz nyomán szisztematikus, szakaszonkénti időmérési diagnosztika (nem találgatás) tárta fel a tényleges szűk keresztmetszeteket — ezek egyike sem esett egybe az eredetileg gyanított pontokkal (Gap/Backplate Engine), ami alátámasztja a mérés-előbb-javítás-utóbb elvet:

* **Numbering Engine** — a kimerítő fallback-keresés (amikor a gyors sarok-illesztés nem talál pozíciót) sorrend-vak, teljes rácsot bejáró implementációja akár egyszerű modelleknél is a teljes futásidő 80%-át tette ki. Javítás: távolság szerint táguló, korai megállásos bejárás (`_iter_grid_points_by_distance()`), bizonyítottan azonos kimenettel. ~53× gyorsulás a mért forgatókönyvön.
* **Dowel Engine** — az automatikus jelölt-generálás (`_longest_run()`) rácspontonként, Python-szinten, minden szeletre egyenként tesztelt — nagy szeletszámnál ez dominált (88,7%, 19 s). Javítás: `shapely.contains_xy()`/NumPy-alapú vektorizálás, bizonyítottan azonos kimenettel (referencia-implementációval bitre pontosan összevetve). ~28× gyorsulás.
* **GUI-előnézet (ADR-0011, ADR-0012)** — a 3D-előnézet geometria-építése a fő szálon, szinkron futott, UI-fagyasztó módon — mind a Futtatás utáni első megjelenítésnél, mind a kiemelés-/nézet-váltásnál. Javítás: a geometria-építés (tiszta, Qt-független `render_geometry.py`-hívások) háttérszálra vitele; a kiemelés-/nézet-váltásnál egy könnyű generáció-számláló védi ki az elavult eredmények felülírását, publikus jelzés-elkülönítéssel a `MainWindow`-tól (elkerülve egy Futtatás-közbeni interakcióból eredő korrektségi hibát).

Mindhárom élő teszttel megerősítve. Összesített hatás a diagnosztikai forgatókönyveken: "Egyszerű, TIPIKUS" 1911 ms → 368 ms; "Komplex, TIPIKUS" 21 462 ms → 3299 ms.

---

### Phase 7 – Kiegészítő funkciók és felhasználói felület fejlesztése

Állapot: ✅ Approved

Feladata:

* ~~7.1 – 2D export-előnézet: a Nesting Engine kimenetének (exportra kerülő vektorok) 2D megjelenítése ellenőrzés céljából, futtatás/export előtt. Önálló specifikáció + implementáció.~~ — kész (`EXPORT_PREVIEW_SPEC.md`; `preview_panel.py`/`main_window.py`; 10+ új teszt; élő teszttel megerősítve)
* ~~7.2 – Dedikált "Példák megnyitása" GUI-funkció: az `examples/` mappa tartalmának közvetlen elérése, a jelenlegi általános "Fájl → Projekt megnyitása..." dialógus mellett/helyett. Önálló specifikáció + implementáció.~~ — kész (`EXAMPLES_LAUNCHER_SPEC.md`; `examples_dialog.py`/`main_window.py`; 22 új teszt; élő teszttel megerősítve)
* ~~7.3 – Nesting — true-shape elrendezés: a jelenlegi, tengelyfüggő befoglaló téglalapokkal dolgozó csomagolási algoritmus (ADR-0008) lecserélése a tényleges alkatrész-kontúrt figyelembe vevő elrendezésre. ADR-0008-at felváltó új ADR + implementáció (a `NESTING_SPEC.md` — menet közben felfedezett módon — nem igényel módosítást, mert a specifikáció szövege mindvégig ezt az állapotot írta elő).~~ — kész (`ADR-0013`; `nesting_engine.py` BLF-átírása; 5 új teszt; élő teszttel megerősítve)
* ~~7.4 – GUI-átalakítás: a `ParameterPanel` 7 funkciócsoportja és a `RunPanel`-ból kiemelt DXF Export paraméterek együtt 8, fenti fülsávos (tab), lapozható kártyává alakulnak (a Dowel/Gap/Backplate be-/kikapcsolók a saját kártyájukon belül maradnak); a log/állapot-terület kiemelése a `RunPanel`-ból önálló, húzással állítható magasságú `QSplitter`-panelbe; a Futtatás/DXF Export gombok áthelyezése a `ParameterPanel` oszlop aljára.~~ — kész (`parameter_panel.py`/`run_panel.py`/`main_window.py` átalakítása; a Futtatás gomb, a szeletelt összeállítás alap-teste és élszíne vizuálisan finomítva; élő teszttel megerősítve)

Kilépési feltétel: mind a négy tételhez jóváhagyott dokumentáció (7.1–7.2: specifikáció; 7.3: ADR, a `NESTING_SPEC.md` módosítása nélkül; 7.4: nincs önálló dokumentum, a terv a ROADMAP-ban, a "folytatás 21" megjegyzésben rögzítve) és automatizált teszttel lefedett, élőben megerősített implementáció készül.

---

### Phase 8 – Relief Generator Plugin (opcionális MeshSource plugin)

Állapot: ✅ Approved

Feladata:

* ~~MeshSource architekturális döntés véglegesítése (a jelenleg `docs/drafts/relief_generator_plugin/` alatti `ADR_MESH_SOURCE.md`, `MESH_SOURCE.md`, `ARCHITECTURE_MESH_SOURCE_CHANGE_PLAN.md`, `DOMAIN_MODEL_CHANGE_PLAN.md` tartalmának átvezetése a végleges `docs/adr/`, `ARCHITECTURE.md`, `DOMAIN_MODEL.md` dokumentumokba)~~ — kész (ADR-0014, `ARCHITECTURE.md`, `MESH_SOURCE.md`)
* ~~az opcionális plugin-architektúra ADR-ben rögzítése (`ADR_OPTIONAL_MESHSOURCE_PLUGIN.md` alapján)~~ — kész (ADR-0015)
* ~~a Relief Generator plugin domain-, geometria- és mesh-generálási modelljének véglegesítése (`RELIEF_GENERATOR_DOMAIN.md`, `Wave Function Model.md`, `RELIEF_GEOMETRY_MODEL.md`, `MESH_GENERATION_MODEL.md`, `PARAMETRIC_RELIEF_GENERATOR.md` alapján), a végleges `docs/plugins/relief_generator/` alá~~ — kész, mind a hat dokumentum Elfogadva státuszban
* ~~a plugin repository- és package-struktúrájának véglegesítése (`PLUGIN_REPOSITORY_STRUCTURE_PROPOSAL.md` alapján) és a `plugins/relief_generator/` könyvtár létrehozása~~ — kész (ADR-0016)
* ~~az első implementáció (`IMPLEMENTATION_PLAN.md` alapján): Wave Generator → Height Field → ReliefGeometry → Mesh Generator → Mesh → MeshSource, automatizált teszttel lefedve~~ — kész
* ~~a MeshSource pluginok discovery-mechanizmusának és a hozzá tartozó generikus GUI paraméter-sémának (`MeshSourceDescriptor`) bevezetése, a projektgazda kifejezett kérésére, a valódi Qt GUI-integráció előfeltételeként (ADR-0017)~~ — kész: entry-point alapú discovery (`mesh_source_registry.py`), valódi Qt GUI-integráció a `ParameterPanel`-ban (Forrás-választó, generikus `ParameterSpec`-form, "Generálás" gomb, háttérszálas `_MeshGenerationWorker`)
* ~~élő tesztelés a SliceDesigner meglévő pipeline-jával~~ — kész, a projektgazda élő tesztelésével megerősítve: a generált modell hibátlanul végigfut a teljes downstream pipeline-on

Kilépési feltétel: a Relief Generator plugin end-to-end működik (Wave Generator-től a SliceDesigner Slice Engine-jéig), automatizált teszttel lefedve, élő teszttel megerősítve, és minden érintett, korábban `docs/drafts/` alatti dokumentum a végleges helyére került, `Tervezet` helyett `Elfogadva` státusszal.

---

### Phase 9 – Wave Extension (matematikai hullámgenerátor bővítése)

Állapot: ✅ Approved

Feladata:

* ~~9.1 — Wave model extension (domain contract: Wave, WaveFunction, PropagationModel, AmplitudeEnvelope komponensmodell, WaveSet, backward compatibility) — tervezet elfogadva, `docs/plugins/relief_generator/WAVE_DOMAIN_MODEL.md` alá véglegesítve~~ — kész: `plugins/relief_generator/domain/wave.py` (Sinusoidal, DirectionalPropagation, UniformEnvelope, Wave, WaveSet), a `WaveGenerator` refaktorálva a komponensmodellre, automatizált teszttel lefedve (87/87 teszt zöld a `tests/plugins/relief_generator/` alatt, a meglévő tesztek változatlanul), backward compatibility empirikusan igazolva
* ~~9.2 — Spatial amplitude envelope (Radial envelope, Linear/Smooth/Gaussian falloff) — tervezet elfogadva, `docs/plugins/relief_generator/AMPLITUDE_ENVELOPE.md` alá véglegesítve~~ — kész: `plugins/relief_generator/domain/amplitude_envelope.py` (LinearFalloff, SmoothFalloff, GaussianFalloff, RadialAmplitudeEnvelope), tisztán additív (a `WaveGenerator`/`wave.py` érintetlen), automatizált teszttel lefedve (102/102 teszt zöld a `tests/plugins/relief_generator/` alatt)
* ~~9.3 — Radial wave source (RadialPropagation) — tervezet elfogadva, `docs/plugins/relief_generator/RADIAL_WAVE_SOURCE.md` alá véglegesítve~~ — kész: `plugins/relief_generator/domain/radial_wave_source.py` (RadialPropagation, invariáns nélkül), tisztán additív (a `WaveGenerator`/`wave.py`/`exceptions.py` érintetlen), automatizált teszttel lefedve (106/106 teszt zöld a `tests/plugins/relief_generator/` alatt)
* ~~9.4 — Multiple wave sources (explicit, felhasználói szinten megadható hullámforrás-lista) — tervezet elfogadva, `docs/plugins/relief_generator/MULTIPLE_WAVE_SOURCES.md` alá véglegesítve~~ — kész: `plugins/relief_generator/domain/multiple_wave_sources.py` (WaveSourceSpec, build_wave, build_combined_wave_set), tisztán additív (a `WaveGenerator`/`WaveParameters` érintetlen — a tényleges GUI-bekötés a 9.7 hatóköre), automatizált teszttel lefedve (126/126 teszt zöld a `tests/plugins/relief_generator/` alatt)
* ~~9.5 — Wave combination / weighting (weight szemantika) — tervezet elfogadva, `docs/plugins/relief_generator/WAVE_WEIGHTING.md` alá véglegesítve~~ — kész: a domain-contract már a 9.1-ben (`Wave.weight`, `WaveSet` súlyozott összegzése) teljesen implementálva volt; a 9.5 kizárólag ennek dedikált, `WaveSet`-szintű tesztlefedettségét adta hozzá (`tests/plugins/relief_generator/domain/test_wave_weighting.py`, production kód módosítása nélkül), automatizált teszttel lefedve (111/111 teszt zöld a `tests/plugins/relief_generator/` alatt)
* ~~9.6 — Controlled procedural distortion (koordináta-torzítás, önálló Distortion komponens) — tervezet elfogadva, `docs/plugins/relief_generator/PROCEDURAL_DISTORTION.md` alá véglegesítve~~ — kész: `Distortion` Protocol és `distortion: Distortion | None = None` mező a `wave.py`-ban (9.1 óta első módosítása), `plugins/relief_generator/domain/procedural_distortion.py` (SwirlDistortion), a teljes 9.1–9.5 tesztkészlet módosítás nélkül zöld maradt (backward compatibility empirikusan igazolva), automatizált teszttel lefedve (136/136 teszt zöld a `tests/plugins/relief_generator/` alatt) — **a Phase 9 mind a hat domain-contractja (9.1–9.6) ezzel kész, hátra van a 9.7 (Integration)**
* ~~9.7 — Integration, validation, documentation (a 9.1–9.6 domain-contract Python-implementációja a meglévő WaveGenerator/WaveParameters kiterjesztésével; automatizált teszt minden alfázisra — RadialPropagation, AmplitudeEnvelope falloff-modellek, WaveSet többforrás-összegzés és weight-szemantika, SwirlDistortion; backward compatibility teszt a Phase 8 WaveGenerator ellenében; end-to-end integrációs teszt a SliceDesigner pipeline-nal; élő tesztelés a projektgazdával) — az implementációs sorrend és tesztelési stratégia `docs/plugins/relief_generator/WAVE_EXTENSION_IMPLEMENTATION_PLAN.md` alá véglegesítve~~ — kész: a `WaveParameters`/`WaveGenerator` teljes bekötése (envelope/distortion/sources/`include_automatic`), a `registration.py` GUI-bővítés, az end-to-end integrációs teszt a Slice Engine-ig, és a projektgazda többkörös élő tesztelése — beleértve a súlyozás tisztázását, a megosztott envelope/distortion + `include_automatic` felülbírálási kört (`MULTIPLE_WAVE_SOURCES.md` 9. szakasza) és a 3D előnézet fényforrás-hibájának javítását (`_clear_plotter()`) — mindet megerősítette

Kilépési feltétel: mind a hét tétel elkészült, a 9.1–9.6 tervezési dokumentumai `docs/drafts/relief_generator_wave_extension/` alól átkerültek a végleges `docs/plugins/relief_generator/` alá Elfogadva státusszal, az implementáció automatizált teszttel lefedve és a projektgazda élő tesztelésével megerősítve.

---

### Phase 10 – Hullámforma-variánsok és eljárásos zajmoduláció

Állapot: ✅ Approved

Feladata:

* ~~10.1 — A generikus `ParameterSpec`/GUI-form vizuális csoportosítása (szakaszolt/összecsukható mezőcsoportok, a meglévő `_CollapsibleSection` mintájára) — `BACKLOG.md` 5. tétel~~ — kész: `ParameterSpec` új `group: str | None = None` mezővel bővült (ADR-0017 kiegészítés), a `_GeneratorParameterForm` az azonos csoportú mezőket egy közös, alapból csukott `_CollapsibleSection`-be rendezi; a relief_generator `_PARAMETERS`-e három csoportot kapott ("Automatikus hullám", "Envelope", "Torzítás"). Kiegészítésként a "Generálás" gomb is fix footerré vált (`ParameterPanel._add_tab()` új `footer` paraméter), a görgethető formtól függetlenül mindig látható. Teljes tesztkészlet (520 teszt) regresszió nélkül zöld.
* ~~10.2 — Alternatív `WaveFunction`-ök (Cosine, Triangle, Sawtooth stb.), kiterjesztve a `WaveSourceSpec`-re is, nem csak az automatikus generálásra — `BACKLOG.md` 1. tétel~~ — kész: a Cosine helyett — mivel matematikailag megegyezik egy fázis-eltolt Sinusoidal-lal — a klasszikus Sine/Triangle/Sawtooth/Square négyes valósult meg. Három új `WaveFunction` (`wave.py`), `WaveParameters.function` (egységes, minden automatikus komponensre) és `WaveSourceSpec.function` (forrásonként egyedi) mező, mindkettő `"Sinusoidal"` alapértékkel — backward compatible. GUI-bekötés a 10.1 csoportosítására építve. A `WAVE_DOMAIN_MODEL.md` 6.3/16.1 és a `MULTIPLE_WAVE_SOURCES.md` 4. szakasza frissült. Teljes tesztkészlet (564 teszt) regresszió nélkül zöld.
* ~~10.3 — Explicit hullámforrások `irregularity`/`complexity`-je, koncentrikus rétegzéssel — `BACKLOG.md` 6. tétel~~ — kész: a `MULTIPLE_WAVE_SOURCES.md` korábbi, kifejezett korlátozása feloldva. Új, megosztott domain-modul (`deterministic_components.py`) vonja össze a `WAVE_FUNCTION_MODEL.md` 22. szakaszának determinisztikus komponens-szabályát, amit mostantól a `WaveGenerator` (automatikus generálás) ÉS a `multiple_wave_sources.build_waves()` (explicit forrás) is használ. A `WaveSourceSpec` új `irregularity`/`complexity` mezőt kapott (mindkettő `0.0` alapértékkel — backward compatible); `build_wave()` átnevezve `build_waves() -> tuple[Wave, ...]`-re, koncentrikus rétegzéssel (azonos `direction`/pozíció minden rétegen, csak amplitúdó/hullámhossz/fázis csökken/jitterel rétegenként). GUI-bekötés a `sources` listában, csoport nélkül. Teljes tesztkészlet (591 teszt) regresszió nélkül zöld.
* ~~10.4 — Megosztott eljárásos zajmező-primitív (Perlin/Simplex-szerű), a 10.5 és 10.6 közös alapja~~ — kész: a tétel a `BACKLOG.md` 3./4. tételéből eredő, eredeti tervnél gazdagabban valósult meg: két önálló, determinisztikus primitívum — `GradientNoiseField` (Perlin-szerű, gradiens-alapú, `[-1,1]`, opcionális `octaves` fraktál-kombinációval) és `VoronoiNoiseField` (cellás/Worley F1-távolság, `[0,1]`) —, mindkettő tisztán aritmetikai, `random` modul nélkül. A megbeszélés során felmerült, hogy a `BACKLOG.md` 1. (megmaradt) tétele (recept-generátorok: dűne, holdkráter, faerezet) erre a gazdagabb primitívum-készletre épülhetne — a projektgazda döntése alapján ez NEM része a Phase 10-nek, továbbra is önálló, jövőbeli fázis marad, de a primitívumok immár készen állnak az alapjául. Nincs GUI-kitettség, nincs `AmplitudeEnvelope`/`Distortion`-becsomagolás — az a 10.5/10.6 hatóköre. Új domain-contract dokumentum: `PROCEDURAL_NOISE.md`. Teljes tesztkészlet (274 teszt a relief_generator alatt) regresszió nélkül zöld.
* ~~10.5 — Amplitude distortion mint `AmplitudeEnvelope`-bővítés, a 10.4-re építve — `BACKLOG.md` 3. tétel~~ — kész: a 10.4-es `GradientNoiseField`/`VoronoiNoiseField` első tényleges becsomagolása — egy apró, helyben (`amplitude_envelope.py`) definiált `NoiseSource` Protocol-lal (`sample(x,y)->float`), amit mindkét primitívum módosítás nélkül kielégít. Az új `NoiseAmplitudeEnvelope` egy `[input_min,input_max]` tartományból lineárisan `[0,1]`-re képez (`VoronoiNoiseField`-hez alapból nem kell remap, `GradientNoiseField`-hez a GUI `-1.0`/`1.0`-t ad át). GUI-bekötés: az `envelope_type` harmadik választása `"Noise"`, egy belső `envelope_noise_type` (Gradient/Voronoi) almezővel, hat új "Envelope" csoportos paraméterrel. Egy apró tanulság: a `AMPLITUDE_ENVELOPE.md` már tartalmazott egy, a promptból nem ismert 10. szakaszt — Claude Code helyesen megállt, jelezte az eltérést, és a projektgazda döntése alapján 11/12-re számozta az új tartalmat; egy emiatt elavult, három helyen előforduló kódbeli kereszthivatkozást (`"10. szakasz"` → `"11. szakasz"`) külön, célzott prompttal javítottunk. Teljes tesztkészlet (286 teszt) regresszió nélkül zöld.
* ~~10.6 — További `Distortion`-típusok, a 10.4-re építve — `BACKLOG.md` 4. tétel~~ — kész: `NoiseDistortion` — a `Distortion` Protocol harmadik megvalósítása, kizárólag `GradientNoiseField`-et használva (NEM `VoronoiNoiseField`-et: koordináta-eltoláshoz előjeles `[-1,1]` érték kell). Klasszikus "domain warping": az X/Y eltolást két, dekorrelált (`seed`/`seed+1`) `GradientNoiseField` adja. Helyben definiált `NoiseSource` Protocol (nem import az `amplitude_envelope.py`-ból, a `Falloff` mintáját folytatva). A "több lépték kombinálása" (`BACKLOG.md` 4. tétel másik fele) a meglévő `octaves` paraméteren keresztül ingyen megvan. GUI-bekötés: `distortion_type` harmadik választása `"Noise"`, hat új "Torzítás" csoportos paraméterrel, nincs típusválasztó. A `PROCEDURAL_DISTORTION.md` 7. szakasza mindkét, korábban "Hatókörön kívül" jelzett pontját (amplitude distortion, további Distortion-típusok) "megvalósítva"-ra jelöltük. Teljes tesztkészlet (292 teszt) regresszió nélkül zöld, `ruff`/`mypy --strict --explicit-package-bases` hibamentes.

Kilépési feltétel: mind a hat tétel (10.1–10.6) elkészült, a szükséges domain-contract dokumentumok Elfogadva státusszal a végleges helyükön vannak, az implementáció automatizált teszttel lefedve és a projektgazda élő tesztelésével megerősítve.

---

## Általános szabályok

* Egyszerre csak egy fázis lehet aktív.
* A következő fázis csak az előző lezárása után kezdhető meg.
* Locked állapotú fázis kizárólag Architecture Decision Record alapján módosítható.
* A ROADMAP a projekt fejlesztési sorrendjének hivatalos nyilvántartása.
